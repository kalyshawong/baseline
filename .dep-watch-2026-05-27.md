# Baseline dependency watch — 2026-05-27

## Critical / high security advisories

| Pkg | Severity | Issues | Fix |
|---|---|---|---|
| `next` 15.5.14 | **HIGH** (×7) + moderate/low (×7) | DoS via Server Components, SSRF via WebSocket upgrades, middleware/proxy bypass (segment-prefetch, dynamic route param injection, i18n), DoS Image Optimization, XSS via CSP nonces / beforeInteractive, RSC cache poisoning, redirect cache poisoning | `npm audit fix` → bumps to **15.5.18** (same major, non-breaking) |
| `postcss` <8.5.10 (transitive) | moderate | XSS via unescaped `</style>` in stringify | resolved by the same `npm audit fix` (pulled in via next's bump) |
| `@anthropic-ai/sdk` 0.82.0 | moderate | Insecure default file permissions in Local Filesystem Memory tool (GHSA-p7fg-763f-g4gf) | requires major bump to **0.98.0** — `npm audit fix --force` or manually bump. **Not auto-fixed** by plain `npm audit fix`. You don't appear to use the Filesystem Memory tool, so this is low real-world risk for Baseline. |

**Recommended commands:**
```
npm audit fix                                  # fixes next + postcss (no breaking changes)
npm install @anthropic-ai/sdk@^0.98.0          # separate, semver-major; review SDK changelog
```

## Major version upgrades available

Ranked by upgrade effort for *this* project (Next 15 App Router, Prisma 6, recharts charts, TS 5.9):

| Pkg | Current → Latest | Effort | One-line breaking-change note |
|---|---|---|---|
| `typescript` | 5.9.3 → 6.0.3 | **LOW** | Transition release toward TS 7 native port; only meaningful removal is `moduleResolution: classic` (Baseline uses `bundler`/`nodenext` per Next defaults). |
| `@types/node` | 22.19.17 → 25.9.1 | **LOW** | Tracks Node runtime version; pin to whatever Node `.nvmrc`/Vercel runtime uses (Node 22 LTS → stay on `@types/node@22`). Skip. |
| `@anthropic-ai/sdk` | 0.82.0 → 0.98.0 | **LOW–MED** | 16 pre-1.0 minors of API drift (`messages` streaming, beta surface area, tool types); spot-check the 1 call site in `src/`. Also closes the moderate advisory. |
| `recharts` | 2.15.4 → 3.8.1 | **MED** | `CategoricalChartState` no longer passed to event handlers / `<Customized>`; props on `<Customized>` and chart event payloads change shape — every chart that reads internal state needs migration ([wiki](https://github.com/recharts/recharts/wiki/3.0-migration-guide)). |
| `@prisma/client` / `prisma` | 6.19.3 → 7.8.0 | **MED** | `new PrismaClient({ datasources })`, `{ datasourceUrl }`, no-arg, and `{}` constructors are removed; pooling options moved. Likely a single `lib/prisma.ts` change in Baseline. |
| `next` | 15.5.14 → 16.2.6 | **HIGH** | Async `params`/`searchParams` everywhere, `next/image` defaults changed (quality/sizes), `middleware.ts` deprecated → rename to `proxy.ts`, PPR config removed (now "Cache Components"), AMP removed, Node 20.9+ / TS 5.1+ minimums. Codemod exists (`npx @next/codemod@canary upgrade latest`) but every dynamic route's `params` access must be `await`-ed. |

## Recommendation

1. **Today** (security, zero-risk): `npm audit fix` — kills 14 of 15 advisories including all 7 high-severity Next CVEs.
2. **This week** (security, semver-major, low risk for you): bump `@anthropic-ai/sdk` to `^0.98.0` and re-run the one Claude code path to confirm streaming still works.
3. **Defer**: TS 6, recharts 3, Prisma 7, Next 16 — none urgent. If you do tackle one, do **Prisma 7 first** (smallest surface), then **recharts 3** (visible regressions easy to spot), then **Next 16** as its own focused session because of `params` async ripple.

Skipped: tailwindcss 4.2→4.3, @tailwindcss/postcss 4.2→4.3, react/react-dom 19.2.4→19.2.6, @types/react 19.2.14→19.2.15 — patches/minors with no advisories.
