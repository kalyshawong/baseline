# Baseline — Documentation Drift Report

**Generated:** 2026-04-29 (automated scheduled task)
**Source-of-truth docs checked:** `docs/PRD.md` (v0.3, last edited 2026-04-06), `docs/architecture.md` (last edited 2026-04-06), `docs/features.md` (last edited 2026-04-23).
**Codebase commit state:** working tree at `/Users/kalysha/projects/baseline` as of run time.

---

## Summary

| Category | Count |
|---|---|
| Documented but missing or changed (broken promises) | **15** |
| Shipped but undocumented (silent features) | **19** |

### Critical-path callouts

- **Oura sync** — PRD §4.1 table only lists 5 Oura endpoints; `src/lib/sync.ts` actually pulls 11. Six endpoints (`daily_spo2`, `enhanced_tag`, `workout`, `session`, `sleep_time`, `daily_resilience`) are silently ingested. (`vo2_max` is wired but commented out.)
- **Baseline Score calculation** — `src/lib/baseline-score.ts` matches PRD §3.1 and architecture §"Baseline Score Calculation" exactly: 40/25/20/15 weights, null-component redistribution, luteal −0.4 °C / ovulation −0.15 °C cycle-aware offsets. **No drift.**
- **Coach context builder** — PRD §3.4 claims "541-line context builder"; actual `src/lib/coach-context.ts` is **1,233 lines**. architecture.md `lib/` description says "14-query context builder"; actual file makes **22 `prisma.*.find/count` calls across 19 distinct models**. Behavior described in features.md (goal lenses, section ordering, primary-goal frame) is shipped but not reflected anywhere in PRD/architecture.
- **Cycle-phase logic** — Temperature-deviation scoring in `tempDeviationScore()` matches docs (luteal −0.4, ovulation −0.15). ACL warning during ovulation is shipped (`cyclePhaseGuidance("ovulation").aclWarning = true`, rendered in `cycle-phase-guidance-card.tsx`) — contradicts PRD §3.3 "not yet shipped" list. **PRD is stale; code is correct.**
- **Auth / OAuth flow** — `/api/auth/oura/route.ts` builds the authorize URL with a CSRF cookie; `/api/auth/oura/callback/route.ts` exchanges the code; `getValidToken()` in `src/lib/oura.ts` handles refresh. Matches PRD §4.1 and architecture's "External Integrations". **No drift.**

---

## Documented but missing or changed

### 1. PRD §3.3 "Body Mode — Features (not yet shipped)" — **all seven items are shipped**

PRD lines 91–99 list these as not yet shipped, but every one is implemented and rendered in the UI today:

| Doc claim (PRD line) | Status | Evidence |
|---|---|---|
| L93 "HRV CV as overreaching signal (UI not connected)" | **Shipped** | `src/components/body/readiness-tier-card.tsx` lines 41–81: `hrvCvElevated = hrvCv > 10`, renders "HRV unstable" badge, displays `HRV CV: {hrvCv.toFixed(1)}%`, and shows the full Flatt & Esco overreaching warning block. |
| L94 "Deload recommendation engine (logic exists, no UI trigger)" | **Shipped** | `src/app/body/page.tsx` calls `computeFatigueScore({...})` and renders the fatigue/deload signal card with "X consecutive training weeks (deload every 5-6 per Pritchard)" copy. |
| L95 "ACL injury risk flag during ovulation (Hewett: 3–6× higher)" | **Shipped** | `src/lib/training.ts` `cyclePhaseGuidance("ovulation")` returns `aclWarning: true`. `src/components/body/cycle-phase-guidance-card.tsx` renders the "ACL caution" badge and the full "ACL injury risk is 3-6x higher during ovulation. 72.5% of ACL injuries..." block. |
| L96 "Volume zone display (MEV/MAV/MRV per muscle group)" | **Shipped** | `src/components/body/volume-zones.tsx` renders MEV/MAV/MRV gauges for all 10 muscle groups defined in `volumeZones` (`src/lib/training.ts` lines 29–40). |
| L97 "Weekly sets per muscle group tracking" | **Shipped** | Same `volume-zones.tsx` component plus `compoundContributions` mapping in `training.ts` lines 301–325. |
| L98 "Energy availability warning (<30 kcal/kg FFM)" | **Shipped** | `src/components/weight/tdee-card.tsx`: `const lowEA = energyAvailability != null && energyAvailability < 30;` with the "EA: X kcal/kg FFM. Below 30 impairs recovery" warning block. Backed by `energyAvailability()` in `training.ts`. |
| L99 "Per-meal protein flag (>30g threshold)" | **Shipped** | `src/components/body/nutrition-check.tsx` iterates `nutrition.perMealProtein` and flags `excess = m.protein > 30` (amber) and `low = m.protein < 20` (yellow). Backed by `perMealProteinStatus()` in `training.ts`. |

### 2. PRD §7 Roadmap "Phase 2b" — two unchecked items are shipped

PRD line 266: `- [ ] Volume zone alerts (MEV/MAV/MRV)` — shipped (see above).
PRD line 267: `- [ ] Deload detection UI` — shipped (see above).

### 3. PRD §3.5 Goals — goal type taxonomy mismatch

PRD line 119: "Goal types: weight, race, exam, performance, habit, custom".
Actual implementation in `src/components/goals/goals-manager.tsx` lines 11–34 uses **`race | strength | physique | cognitive | weight | health | custom`** with their own subtypes. `prisma/schema.prisma` `Goal.type` comment lines 269–270 also documents the actual set. The PRD's `exam`, `performance`, `habit` types do not exist; `strength`, `physique`, `cognitive`, `health` types are missing from PRD.

### 4. PRD §3.4 Coach — context-builder line count is stale

PRD line 109: "Rich context aggregation (541-line context builder pulling from all data sources)".
Actual `src/lib/coach-context.ts` is **1,233 lines**. The shipped builder also includes a goal-lens system (sectionOrder, coachingFrame), trade-off detection (`detectTradeoffs`), Hyrox plan integration, pace-gap analysis, and primary-goal lensing — none reflected in PRD §3.4.

### 5. PRD §4.1 — Oura endpoints table is incomplete

PRD lines 156–162 list only 5 endpoints (`daily_readiness`, `daily_sleep`, `daily_activity`, `heartrate`, `daily_stress`).
Actual `src/lib/sync.ts` also calls (verified by grep `ouraFetch`): `daily_spo2`, `enhanced_tag`, `workout`, `session`, `sleep_time`, `daily_resilience`. (`vo2_max` is referenced in commented-out code at `src/lib/sync.ts`.)

### 6. PRD §4.5 + §3.7 — Apple HealthKit listed as Phase 3 / planned, but partially shipped

PRD line 187: "Apple HealthKit (Planned — Phase 3)".
PRD line 147: "V2 — Auto-sync (planned): Pull menstrual data from Apple HealthKit".
Reality: `src/app/api/healthkit-sync/route.ts`, `src/app/api/workouts/apple-watch/route.ts`, and schema models `HealthKitSync`, `HealthKitWorkout`, `HeartRateZoneSummary` are all live. `src/components/dashboard/healthkit-status.tsx` renders sync status on the dashboard. **However**, menstrual auto-sync from HealthKit is *not* shipped — `CyclePhaseLog.source` defaults to `"manual"`; no code path writes the `"healthkit"` source value. So PRD is correct about cycle auto-sync but wrong about HealthKit overall being Phase-3-planned.

### 7. architecture.md §"Application Structure" — Prisma model count and tree

architecture.md line 76: `schema.prisma          # 18 models, ~300 lines`.
Actual `prisma/schema.prisma` has **39 models** and **582 lines** (verified `grep -c '^model ' = 39`, `wc -l = 582`).

### 8. architecture.md §"Database Schema (18 models)" — missing models

The doc heading claims 18 models and lists 23. Both are wrong. The following models exist in `schema.prisma` but are not listed at all in architecture.md §"Database Schema":

- `HyroxPlan`, `HyroxSession`, `HyroxStationBenchmark`, `GoalWorkoutTag` (Hyrox module)
- `HealthKitSync`, `HealthKitWorkout`, `HeartRateZoneSummary` (Apple HealthKit ingest)
- `DailySpO2`, `OuraWorkout`, `OuraSession`, `SleepTimeRecommendation`, `DailyResilience`, `DailyRunningMetrics`, `DailyVO2Max` (Oura expansion)
- `LifeContextDef`, `LifeContextLog` (Life Context module — entirely undocumented anywhere)

### 9. architecture.md §"Application Structure" tree — missing routes and components

architecture.md lines 73–147 omit these existing files:

- Pages: `src/app/body/hyrox/page.tsx`, `src/app/mind/experiments/new/page.tsx`, `src/app/mind/experiments/[id]/page.tsx`.
- API routes: `/api/hyrox/plan`, `/api/hyrox/plan/[id]`, `/api/hyrox/today`, `/api/healthkit-sync`, `/api/workouts/apple-watch`, `/api/workouts/[id]/goals`, `/api/workouts/rpe-suggestions`, `/api/coach/tradeoffs`, `/api/coach/sessions`, `/api/coach/sessions/[id]`, `/api/life-context/defs`, `/api/life-context/logs`.
- Components: `src/components/hyrox-summary-card.tsx`, `src/components/dashboard/healthkit-status.tsx`, `src/components/mind/life-context-card.tsx`, `src/components/body/running-metrics-card.tsx`, `src/components/body/goal-tagger.tsx`, `src/components/body/nutrition-check.tsx`, `src/components/goals/countdown-ring.tsx`.
- Library files: `src/lib/anthropic-retry.ts`, `src/lib/utils.ts`, `src/lib/hyrox-archive.ts`, `src/lib/hyrox-blocks.ts`, `src/lib/hyrox-constants.ts`, `src/lib/hyrox-pace.ts`, `src/lib/hyrox-plan-sync.ts`, `src/lib/hyrox-session-recommender.ts`.

### 10. architecture.md §"Application Structure" — `src/app/api` annotations stale

Annotations like "OAuth2 flow (2 routes)", "Workout CRUD + sets + trends (7 routes)" no longer reflect actual route counts. Workouts directory now has **7 route handlers** including `/goals`, `/apple-watch`, `/rpe-suggestions`, `/trends`, plus `/[id]/sets/[setId]` (set-level mutation).

### 11. architecture.md `lib/` annotation for coach-context

architecture.md line 128: `coach-context.ts      # 14-query context builder for AI coach`.
Actual: **22** `prisma.*.find/count` calls across **19** distinct models (counted via grep). The annotation also doesn't reflect goal-lens / trade-off / Hyrox-pace logic.

### 12. architecture.md §"Data Flow — Coach Chat" — stale query count

architecture.md line 217: "buildCoachContext() — 14 Prisma queries aggregating all user data".
Actual count is 22 (see above). Should also note that the system prompt is now goal-lensed via `goalSystemPromptSection()`.

### 13. architecture.md §"Tech Stack" / §"Application Structure" — file counts in component descriptions stale

architecture.md lines 111–116:
- "components/dashboard/ … (7)" — actual 9 files (`activity-card`, `baseline-score-card`, `calorie-balance-card`, `cycle-phase-selector`, `healthkit-status`, `macro-summary`, `metric-card`, `sync-button`, `trend-chart`).
- "components/mind/ … (8)" — actual 9 files (adds `life-context-card.tsx`).
- "components/body/ … (6)" — actual 8 files (adds `goal-tagger.tsx`, `nutrition-check.tsx`, `running-metrics-card.tsx`).
- "components/coach/ … (1)" — correct.
- "components/goals/ … (1)" — actual 2 (`goals-manager.tsx`, `countdown-ring.tsx`).
- "components/weight/ … (5)" — correct.

### 14. PRD §3.7 "V2 Auto-sync — Apple HealthKit / Oura body temp proxy"

The PRD names `Oura body temperature deviation` as a planned proxy. Code uses Oura `temperatureDeviation` only as an input to the existing Baseline Score (not for phase auto-detection). **Unchanged from PRD claim.** Listed here as an "unchanged promise" only because it's actively referenced and worth re-confirming each drift cycle.

### 15. features.md §"Cross-cutting Notes" — references `research/variable-research.md`

features.md lines 4 and 228 reference `research/variable-research.md`. Verified: `research/` directory exists at repo root and contains files. **No drift** but flagged because the doc structure (`research/` vs `docs/`) is split and PRD/architecture both treat `body-mode-research.md` as living under `docs/`. PRD line 295 still cites `body-mode-research.md` as a top-level docs file — actual location is `research/body-mode-research.md`.

---

## Shipped but undocumented

### A. Entire Hyrox race-prep module — undocumented in source-of-truth

Schema: `HyroxPlan`, `HyroxSession`, `HyroxStationBenchmark` models. API: `/api/hyrox/plan`, `/api/hyrox/plan/[id]`, `/api/hyrox/today`. UI: `/body/hyrox/page.tsx`, `src/components/hyrox-summary-card.tsx`. Library: `hyrox-archive.ts`, `hyrox-blocks.ts`, `hyrox-constants.ts`, `hyrox-pace.ts`, `hyrox-plan-sync.ts`, `hyrox-session-recommender.ts`. Coach context integrates Hyrox plan and pace-gap.

PRD.md and architecture.md never mention Hyrox. Only features.md (line 84+) covers it. There is a separate `docs/hyrox-module-spec.md` but PRD §7 Roadmap doesn't reference Hyrox at all.

### B. Entire Life Context module — undocumented anywhere

Schema: `LifeContextDef`, `LifeContextLog` models (`prisma/schema.prisma` lines 547–581). API: `/api/life-context/defs`, `/api/life-context/logs`. UI: `src/components/mind/life-context-card.tsx`. **Not referenced in PRD, architecture, or features.md.**

### C. Apple HealthKit ingest pipeline (active, PRD says planned)

See item #6 above. Schema: `HealthKitSync`, `HealthKitWorkout`, `HeartRateZoneSummary`. API: `/api/healthkit-sync`, `/api/workouts/apple-watch`. UI: `dashboard/healthkit-status.tsx`. Spec: `docs/healthkit-sync-spec.md` exists separately but PRD §4.5 still classifies as Phase-3-planned.

### D. Coach trade-off detection endpoint

`/api/coach/tradeoffs/route.ts` and `detectTradeoffs()` function in `coach-context.ts`. Not in PRD or architecture.

### E. Goal-Workout tagging system

Schema: `GoalWorkoutTag`. API: `/api/workouts/[id]/goals`. UI: `src/components/body/goal-tagger.tsx`. Mentioned in features.md `/body/workout/[id]` section but absent from PRD and architecture.

### F. Daily Running Metrics ingestion + display

Schema: `DailyRunningMetrics` (GCT, vertical oscillation, stride length, cardio recovery, etc.). UI: `src/components/body/running-metrics-card.tsx`. Mentioned in features.md but not PRD/architecture.

### G. VO2 Max tracking

Schema: `DailyVO2Max`. Display logic exists in `/body/page.tsx`. Mentioned in features.md but not PRD or architecture (and the `vo2_max` Oura sync call is currently commented out in `sync.ts`, so there's an additional "documented behaviour without ingestion" sub-issue).

### H. Daily Resilience metric

Schema: `DailyResilience`. Rendered as one of the six dashboard metric cards (`src/app/page.tsx`). Mentioned in features.md but not PRD §3.1 or architecture.

### I. SpO2 metric card

Schema: `DailySpO2`. Rendered as one of the six dashboard metric cards. Mentioned in features.md but missing from PRD §4.1 Oura endpoint table.

### J. Bedtime recommendation card

Schema: `SleepTimeRecommendation`. Rendered on dashboard (`src/app/page.tsx` reads `optimalBedtimeStart/End`). Mentioned in features.md but not PRD/architecture.

### K. Today's sessions card (Oura breathing/meditation/nap)

Schema: `OuraSession`. Read in `src/app/page.tsx` via `prisma.ouraSession.findMany`. Mentioned in features.md but not PRD/architecture.

### L. Oura enhanced tag ingestion

`sync.ts` calls `ouraFetch<...>("enhanced_tag", ...)`; `ActivityTag.ouraTagId` field on schema dedupes them. Not mentioned in PRD §4.1 or architecture.

### M. Oura workout records

Schema: `OuraWorkout`. Pulled in `sync.ts`. Not in docs.

### N. Goal-Coach lens / primary-goal frame system

Substantial feature in `coach-context.ts` (`goalLenses` table, `sectionOrder`, `coachingFrame`, `goalSystemPromptSection`). Documented separately in `docs/goal-coach-redesign-spec.md` and referenced in features.md but **completely absent from PRD §3.4 (Coach) and architecture.md**.

### O. `anthropic-retry.ts` library

Wrapper for Claude API with retry logic — exists at `src/lib/anthropic-retry.ts`. Architecture's `lib/` listing omits it.

### P. `utils.ts` library

`apiError()` and shared helpers — `src/lib/utils.ts`. Architecture's `lib/` listing omits it.

### Q. `/api/profile` endpoint

Listed in architecture but undescribed; PRD has no mention. Underpins UserProfile sex/age/height/etc. used by TDEE math.

### R. Workout `rpe-suggestions` endpoint (PRD says shipped, architecture lacks detail)

PRD §3.3 line 87 mentions "RPE suggestions API" but architecture.md route list never enumerates `/api/workouts/rpe-suggestions/route.ts`. Counts as silent because architecture is the canonical source for routes.

### S. Set-level mutation route

`/api/workouts/[id]/sets/[setId]/route.ts` exists for individual set update/delete; architecture only mentions the collection-level `/sets` endpoint.

---

## Recommendations (prioritized)

1. **Update PRD §3.3 "Features (not yet shipped)" list** — move all 7 items into "shipped" since they all render in production code today. (Highest-impact correction; the list is actively misleading.)
2. **Update PRD §4.1 Oura endpoints table** to include `daily_spo2`, `enhanced_tag`, `workout`, `session`, `sleep_time`, `daily_resilience`. Add a row noting `vo2_max` is implemented but currently commented out.
3. **Update PRD §4.5 / §3.5 / §7** to reflect that Apple HealthKit ingestion (workouts, HR zones) is shipped; only menstrual-data auto-sync remains planned.
4. **Add a Hyrox Mode section to PRD §3** and an entry in architecture.md's Database Schema and tree. The module is a substantial product surface that exists only in features.md and a separate spec.
5. **Add Life Context to PRD and architecture** — currently entirely undocumented.
6. **Refresh architecture.md model count and tree** — the "18 models / ~300 lines" header should become "39 models / 582 lines" and the schema list should be regenerated (or auto-generated from Prisma).
7. **Sync goal-type taxonomy** between PRD §3.5 and the schema/UI.
8. **Refresh coach-context line count and query count** in PRD §3.4 and architecture.md "Coach Chat" data-flow.

---

*Auto-generated drift report. To re-run, trigger the `baseline-doc-drift-detector` scheduled task.*
