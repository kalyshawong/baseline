# Baseline — Documentation Drift Report

**Generated:** 2026-08-13 (automated scheduled run)
**Source-of-truth docs checked:** `docs/PRD.md` (v0.3, updated 2026-04-06), `docs/architecture.md` (2026-04-06), `docs/features.md` (2026-04-23)
**Code checked:** `src/app/`, `src/components/`, `src/lib/`, `prisma/schema.prisma` + `prisma/migrations/`

## Summary

| Category | Count |
|---|---|
| Documented but missing or changed (broken promises) | 12 |
| Shipped but undocumented (silent features) | 12 |

**Headline:** PRD.md and architecture.md have drifted badly — they still describe an 18-model **SQLite single-user** app, but the code has migrated to a **42-model PostgreSQL multi-tenant** schema (migration `20260616222619_init_multitenant` is applied). features.md is much closer to reality but still misstates the HRV-CV threshold and the coach architecture. Every one of the seven items in PRD's "Features (not yet shipped)" list has, in fact, shipped.

Critical-path status (Oura sync, Baseline Score, Coach context, cycle-phase, auth/OAuth) is called out in its own section below.

---

## Documented but missing or changed (broken promises)

### 1. Database is PostgreSQL, not SQLite
- **Doc says:** architecture.md L61 `| **Database** | SQLite via Prisma | Zero-config, embedded, sufficient for single-user.` and L271 "Why SQLite over PostgreSQL"; PRD.md L200 `**Stack:** ... Prisma (SQLite)`.
- **Code says:** `prisma/schema.prisma` L24 `provider = "postgresql"` with Supabase pooled + `directUrl`; migration `prisma/migrations/20260616222619_init_multitenant/migration.sql` uses Postgres `CREATE TYPE ... AS ENUM`. A stale `prisma/dev.db` (SQLite) still sits in the folder, adding to the confusion.
- **Severity:** High — the entire "Why SQLite" design-decision section (architecture.md L271–273) is now false.

### 2. Schema is 42 models (~907 lines), not 18 (~300 lines)
- **Doc says:** architecture.md L76 `│   ├── schema.prisma          # 18 models, ~300 lines` and L151 `## Database Schema (18 models)`.
- **Code says:** `prisma/schema.prisma` has **42** `model` blocks / 907 lines. The documented 18 are all present, but 24 more exist (User, UserBaseline, Hyrox*, HealthKit*, HeartRateZoneSummary, DailySpO2, OuraWorkout, OuraSession, SleepTimeRecommendation, DailyResilience, DailyRunningMetrics, DailyVO2Max, GoalWorkoutTag, LifeContextDef, LifeContextLog, WorkoutNote).

### 3. schema.prisma still self-labels as an un-adopted "DRAFT"
- **Doc says (in-code banner):** `prisma/schema.prisma` L1 `// MULTI-TENANT DRAFT SCHEMA — review before adopting. Drafted June 16, 2026.` and L15 "NOT done here (deliberately): ... the 305-callsite query sweep, and data backfill. This is the shape only."
- **Reality:** The schema has been adopted — migrations are applied and the query sweep is done: e.g. `src/lib/baseline-score.ts` L171 queries `where: { userId_day: { userId: getCurrentUserId(), day: today } }`. The "draft / not adopted" banner is itself drift.

### 4. Coach context builder is ~1,367 lines / ~22 queries, not 541 lines / 14 queries
- **Doc says:** PRD.md L110 `Rich context aggregation (541-line context builder pulling from all data sources)`; architecture.md L128 `│       ├── coach-context.ts       # 14-query context builder for AI coach` and L217 `buildCoachContext() — 14 Prisma queries aggregating all user data`.
- **Code says:** `src/lib/coach-context.ts` is 1,367 lines; `buildCoachContext()` (defined L539) and helpers issue ~22 `prisma.*` calls.

### 5. Coach now uses a tool-use loop — undocumented in the data-flow diagram
- **Doc says:** architecture.md L213–221 "Coach Chat" flow describes only `system prompt + context + conversation history` → stream. No tools.
- **Code says:** `src/app/api/coach/route.ts` L7 imports `COACH_TOOLS, runCoachTool` from `src/lib/coach-tools.ts` and L160 passes `tools: COACH_TOOLS`. There is also an undocumented `src/app/api/coach/tradeoffs/route.ts` + `detectTradeoffs()` (coach-context.ts L240).

### 6. All seven PRD "Features (not yet shipped)" have shipped
PRD.md L91–99 lists these under Body Mode "**Features (not yet shipped):**" — each is now implemented:

- **L93 "HRV CV as overreaching signal (UI not connected)"** → connected. `src/lib/training.ts` `isHrvCvElevated()` (L195) is consumed by `computeTrainingCall` (training-call.ts) which drives `readiness-tier-card.tsx`.
- **L94 "Deload recommendation engine (logic exists, no UI trigger)"** → triggered. `computeFatigueScore()` (training.ts L215, score ≥3 → "Deload recommended") feeds training-call and the `volume-zones.tsx` "consider deload" banner (L60).
- **L95 "ACL injury risk flag during ovulation (Hewett: 3–6× higher)"** → shipped. `src/components/body/cycle-phase-guidance-card.tsx` L35–37 "ACL injury risk is 3-6x higher during ovulation"; also training.ts L362 and coach-context.ts L834.
- **L96 "Volume zone display (MEV/MAV/MRV per muscle group)"** → shipped. `src/components/body/volume-zones.tsx`; `src/lib/training.ts` L20–72.
- **L97 "Weekly sets per muscle group tracking"** → shipped. `src/lib/training-call.ts` L116–133 (`muscleSets`, `volumeApproachingMRV`).
- **L98 "Energy availability warning (<30 kcal/kg FFM)"** → shipped. `training.ts` `energyAvailability()` L255; surfaced in `tdee-card.tsx` L36 `lowEA = ... < 30` and `nutrition-check.tsx` L28.
- **L99 "Per-meal protein flag (>30g threshold)"** → shipped. `training.ts` `perMealProteinStatus()` L246; `nutrition-check.tsx` `perMealProtein` (L9, L73–79).

### 7. Apple HealthKit is partially shipped, documented as "Planned — Phase 3"
- **Doc says:** PRD.md L187 `### 4.5 Apple HealthKit (Planned — Phase 3)`; architecture.md L251 status "Planned (Phase 3)".
- **Code says:** shipped ingestion — `src/app/api/healthkit-sync/route.ts`, `src/app/api/workouts/apple-watch/route.ts`, models `HealthKitSync` / `HealthKitWorkout` / `HeartRateZoneSummary` (schema L654–704), and `src/components/dashboard/healthkit-status.tsx`. features.md L18 already treats HealthKit as live ("last HealthKit sync and any Apple Watch workout") — so PRD/architecture are the laggards.

### 8. Multi-user / Postgres migration listed as Phase 3 "future" is already done at the data layer
- **Doc says:** PRD.md L281 roadmap Phase 3 `- [ ] Multi-user support (PostgreSQL migration)`.
- **Code says:** the Postgres multi-tenant migration is applied (item #1/#2). App logic is still single-tenant, but via a deliberate shim: `src/lib/current-user.ts` returns a hardcoded `SOLO_USER_ID = "usr_kalysha"`. So this roadmap item is ~half shipped and mis-filed as untouched-future.

### 9. Security section assumes single-user / no auth
- **Doc says:** architecture.md L260 `Single-user, localhost-only in Phase 1 — no user auth needed`.
- **Code says:** the schema is now multi-tenant (`User` model, `userId` on every table). Auth itself is not built (SOLO_USER_ID shim), so the *statement* is temporarily still true operationally, but the surrounding "Security Considerations" list (L256–263) no longer reflects the multi-tenant data model and should be revisited before real users land.

### 10. HRV-CV threshold: doc still states flat 10%, code uses a personalized threshold
- **Doc says:** features.md L57 `... (Flatt & Esco 2016; CV > 10% flagged).`
- **Code says:** `src/lib/training.ts` L124–201 deliberately replaced the flat cutoff. `FLAT_HRV_CV_THRESHOLD = 10` (L140) is now only a fallback; `hrvCvThreshold()` (L189) uses the user's own rolling-CV mean + 1 SD (`rollingHrvCvBaseline`, L159). Comment L126 documents the recalibration ("the flat 10% CV cutoff ... reads 'overreaching' every single day"). The doc's "CV > 10% flagged" is stale.

### 11. Experiment analysis: features.md implies Bayesian, code is frequentist (Welch)
- **Doc says:** features.md L136 "Bayesian inference is preferred over frequentist at low n" and L180 same claim.
- **Code says:** `src/lib/correlation.ts` implements Welch's t-test + Cohen's d (as architecture.md L64/L124 correctly states). No Bayesian analysis path exists. features.md oversells the method.

### 12. lib/ inventory in architecture is incomplete / partly mislabeled
- **Doc says:** architecture.md L119–132 lists 14 `lib/` modules.
- **Code says:** `src/lib/` has 34 modules. Undocumented ones include `coach-tools.ts`, `current-user.ts`, `cycle-phase.ts`, `flags.ts`, `today-call.ts`, `training-call.ts`, `fisher.ts`, `food-tags.ts`, `gi-classifier.ts`, `meal-gi.ts`, `pre-workout-fuel.ts`, `workout-notes.ts`, `workout-discussion.ts`, `anthropic-retry.ts`, and the entire `hyrox-*` set. Also `usda.ts` is documented as "Claude-powered macro estimation" (architecture L131) — accurate behavior, but the USDA name is a vestige (it calls Anthropic, not USDA).

---

## Shipped but undocumented (silent features)

Modules/areas present in code with no mention in PRD.md or architecture.md. (Several are described in their own spec docs referenced by features.md, noted where applicable.)

1. **Hyrox race-prep module** — page `src/app/body/hyrox/page.tsx`; routes `src/app/api/hyrox/{plan,plan/[id],sessions,today}`; lib `hyrox-blocks.ts`, `hyrox-pace.ts`, `hyrox-session-recommender.ts`, `hyrox-today.ts`, `hyrox-plan-sync.ts`, `hyrox-constants.ts`, `hyrox-archive.ts`; models `HyroxPlan`/`HyroxSession`/`HyroxStationBenchmark`; components `hyrox-summary-card.tsx`, `dashboard/hyrox-countdown-card.tsx`, `body/start-hyrox-session-button.tsx`. Documented in features.md + `hyrox-module-spec.md` but **entirely absent from PRD/architecture**.

2. **Workout notes + meal→GI backward analyzer** — model `WorkoutNote` (+ `giOutcome`/`giConfidence`/`giEvidence`/`giNeedsReview`, migration `20260617015312_add_gi_fields`); routes `src/app/api/workout-notes/{route,[id],[id]/analyze}`; lib `workout-notes.ts`, `workout-discussion.ts`, `meal-gi.ts` (Fisher's exact), `gi-classifier.ts`, `food-tags.ts`, `pre-workout-fuel.ts`; components `mind/gi-patterns-card.tsx`, `dashboard/workout-notes-block.tsx`. Has `docs/meal-gi-analyzer-spec.md` but not in the three SoT docs.

3. **Life Context tracking** — routes `src/app/api/life-context/{defs,logs}`; models `LifeContextDef`/`LifeContextLog`; component `mind/life-context-card.tsx`.

4. **Per-user personalization / baseline maturity** — models `User` + `UserBaseline` (per-signal mean/sd/cv/maturity gate); `UserProfile.hrvBaselineChoice` field; `src/lib/current-user.ts`; `getHrvBaselineChoice` (training-call.ts). This is the core "priors → personalized" mechanism and is undocumented in architecture.

5. **Oura data expansion (7 tables + cards)** — models `DailySpO2`, `OuraWorkout`, `OuraSession`, `SleepTimeRecommendation`, `DailyResilience`, `DailyRunningMetrics`, `DailyVO2Max`; components `body/running-metrics-card.tsx`, `body/recovery-signals-row.tsx`, plus dashboard SpO2/Resilience/Bedtime cards. features.md documents the UI; architecture's "18 models" schema omits all of them.

6. **"Today's Call" / flags engine** — lib `today-call.ts`, `training-call.ts`, `flags.ts`; components `dashboard/today-call-card.tsx`, `today-call-hero.tsx`, `do-section.tsx`, `tonight-section.tsx`, `mind/flags-feed.tsx`. Not in architecture's business-logic layer.

7. **Coach tools + tradeoffs** — `src/lib/coach-tools.ts` (`COACH_TOOLS`, `runCoachTool`), `src/app/api/coach/tradeoffs/route.ts`, `detectTradeoffs()` (coach-context.ts L240). See broken-promise #5.

8. **Mobile / PWA layer** — `src/components/mobile/*` (11 components: mobile-dashboard, mobile-coach, mobile-goals, mobile-tab-bar, mobile-training-tier, etc.), `src/app/mobile.css`, `src/components/pwa-register.tsx`. PRD lists "Mobile app" only as a Phase 3 future item (L282).

9. **Manual + Apple-Watch workout entry & RPE suggestions** — routes `src/app/api/workouts/manual`, `src/app/api/workouts/apple-watch`, `src/app/api/workouts/rpe-suggestions`; component `dashboard/manual-workout-entry.tsx`. (Architecture's workout flow only documents template/blank → sets.)

10. **Meal timing + source on nutrition** — `NutritionEntry` fields `mealType`, `eatenAt`, `timeUnknown`, and `MealSource` enum (home_cooked/takeout/restaurant/pre_packaged). Architecture describes nutrition only as "daily macro totals" + "per-food breakdown."

11. **Anthropic resilience layer** — `src/lib/anthropic-retry.ts` (`withAnthropicRetry`), used by coach + usda. Undocumented.

12. **Second statistics engine (Fisher's exact)** — `src/lib/fisher.ts`, used by `meal-gi.ts`. Architecture lists only jstat/Welch under Statistics.

---

## Critical-path callouts

- **Oura sync** — ✅ Matches. `src/app/api/sync/route.ts` + `src/lib/sync.ts` + `src/lib/oura.ts` exist and match architecture L192–200 (token refresh → fetch → upsert by day → SyncLog). Note: sync surface has *expanded* silently to SpO2/workouts/sessions/resilience/running/VO2max/sleep-time tables (see silent feature #5).

- **Baseline Score calculation** — ✅ Accurate. `src/lib/baseline-score.ts` `computeBaselineScore()` uses exactly the documented weights (readiness 0.40, HRV 0.25, sleep 0.20, temp 0.15; L98–99) with null-weight redistribution (L102–106), and the documented cycle-aware temp offsets **luteal −0.4°C / ovulation −0.15°C** (L69, L72) — matches PRD L50 and architecture L209. Enhancement not yet doc'd: it's now `userId`-scoped and `cache()`-memoized (L171, L233).

- **Coach context builder** — ⚠️ Drifted. Function is real and central (`buildCoachContext`, coach-context.ts L539) but the doc's "541-line / 14-query" figures are wrong (now ~1,367 lines / ~22 queries) and the coach now runs a **tool-use loop** the architecture doesn't mention. See broken-promises #4 and #5.

- **Cycle-phase logic** — ✅ Present, enhanced. `src/lib/cycle-phase.ts` `resolveCyclePhase()` is used by Baseline Score (baseline-score.ts L216) and adds a staleness guard (returns `phase: null` past a phase's max-days cap) that the docs don't describe. Functionally consistent with "modifier, not override" (features.md L221).

- **Auth / OAuth flow** — ✅ Present, but context changed. `src/app/api/auth/oura/route.ts` + `callback/route.ts` exist and match architecture L96. However architecture's "no user auth needed / single-user" framing (L260) is now at odds with the multi-tenant `User` schema; real end-user auth is stubbed by `SOLO_USER_ID` in `current-user.ts` (broken-promise #8/#9).

---

## Notes / assumptions (autonomous run)

- Treated `prisma/schema.prisma` as the active schema (its migrations are applied and its `userId_day` keys are used in code), despite its stale in-file "DRAFT — review before adopting" banner. Ignored the sibling `schema.draft.prisma` and `schema.sqlite.bak`.
- Verification was by static read + grep, not by running the app. Line numbers reference the files as of this run's read.
- "Undocumented" = absent from PRD.md and architecture.md specifically; some items are covered by other spec docs (hyrox-module-spec.md, meal-gi-analyzer-spec.md, goal-coach-redesign-spec.md) that features.md references. The fix in most cases is to reconcile PRD.md + architecture.md up to where features.md already is.
