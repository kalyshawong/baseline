# Documentation Drift Report — 2026-06-24

**Scope:** Verified `docs/PRD.md` (v0.3, 2026-04-06), `docs/architecture.md` (2026-04-06), and `docs/features.md` (2026-04-23) against `src/app/`, `src/components/`, `src/lib/`, and `prisma/schema.prisma`.

**Headline:** PRD.md and architecture.md are ~2.5 months stale and predate three whole subsystems (Hyrox, Meal/GI analyzer, mobile/PWA UI) plus a large Oura/HealthKit data expansion and multi-user scaffolding. features.md is much more current but still omits several shipped modules and the schema has nearly doubled.

- **Documented but missing or changed:** 11
- **Shipped but undocumented:** 13

No documented feature was found to be *deleted* from code — the drift is overwhelmingly "code ran ahead of docs," not "docs promise vaporware." The one genuine broken promise is the reverse: PRD still lists as **not-yet-shipped** a batch of Body Mode features that are, in fact, fully implemented.

---

## Critical-path summary (read this first)

| Critical path | Status | Note |
|---|---|---|
| **Oura sync** | ✅ Works, **under-documented** | `lib/sync.ts` + `lib/oura.ts` present; `getValidToken()` refresh logic intact (`oura.ts:20,29`). But sync now writes **7 Oura models not listed** in architecture's schema section or PRD §4.1 (SpO2, Resilience, VO2Max, OuraWorkout, OuraSession, SleepTimeRecommendation). |
| **Baseline Score** | ✅ **Matches docs exactly** | `lib/baseline-score.ts` confirms readiness 40 / HRV 25 / sleep 20 / temp 15, null-weight redistribution, and cycle-aware temp offsets (luteal −0.4°C `:69`, ovulation −0.15°C `:72`). No drift. |
| **Coach context builder** | ⚠️ Exists, **doc metrics wrong** | `lib/coach-context.ts` is **1367 lines / 22 prisma queries**, not the documented "541-line" (PRD §3.4) / "14-query" (architecture lines 128 & 217) builder. Also `lib/coach-tools.ts` and route `api/coach/tradeoffs` are undocumented. |
| **Cycle-phase logic** | ⚠️ Manual shipped; auto-sync **partially contradicted** | `lib/cycle-phase.ts` (`resolveCyclePhase`, staleness guards, `getCurrentPeriodDay`) present. PRD §3.7 still calls HealthKit menstrual auto-sync "planned," but the HealthKit sync infra (route + 3 models) now exists. |
| **Auth / OAuth** | ✅ Oura OAuth works; ⚠️ multi-user half-shipped | `api/auth/oura/*` + token refresh present. architecture says "Single-user … no user auth needed," but `lib/current-user.ts` (`getCurrentUserId()`) and `User` + `UserBaseline` models now exist — multi-user scaffolding landed silently with no auth layer yet. |

---

## Documented but missing or changed (broken promises) — 11

### 1. PRD §3.3 "Features (not yet shipped)" list is stale — 7 of 7 items ARE shipped
> PRD.md lines 91–99, "**Features (not yet shipped):**"

Every item on this list is implemented:

| PRD "not yet shipped" line | Reality | Evidence |
|---|---|---|
| "HRV CV as overreaching signal (UI not connected)" | Shipped + UI | `training.ts:113 hrvCV()`; personalized threshold `hrvCvThreshold` / `isHrvCvElevated`; displayed in `components/body/readiness-tier-card.tsx` (features.md: "HRV CV displayed") |
| "Deload recommendation engine (logic exists, no UI trigger)" | Shipped + UI | `training.ts:215 computeFatigueScore()` returns recommendation (`:229–231`); surfaced as "Fatigue signal composite (0–8)" in features.md `/body` |
| "ACL injury risk flag during ovulation (Hewett: 3–6× higher)" | Shipped | `training.ts:362` `aclWarning: true` + note "3-6x higher ACL injury risk (Hewett 2007)" |
| "Volume zone display (MEV/MAV/MRV per muscle group)" | Shipped | `training.ts` `VOLUME_LANDMARKS` + `volumeZones()`; `components/body/volume-zones.tsx` |
| "Weekly sets per muscle group tracking" | Shipped | same (`volume-zones.tsx`) |
| "Energy availability warning (<30 kcal/kg FFM)" | Shipped | `training.ts:255 energyAvailability()` (comment `:254` "Loucks 2011 — below 30 kcal/kg FFM"); coach-context flags `< 30` as `critical` (`coach-context.ts:263`) |
| "Per-meal protein flag (>30g threshold)" | Shipped, **but doc mischaracterizes the threshold** | `training.ts:246 perMealProteinStatus()` flags **low** below an age-based floor (20g <45, 25g 45–64, 30g 65+) and "high" only `> 30`. The actionable flag is the *under-20g* check, not ">30g". |

**Action:** Move these from "not yet shipped" to shipped, and fix the per-meal threshold wording.

### 2. architecture.md: schema model count and size wildly understated
> architecture.md line 76: `│   ├── schema.prisma          # 18 models, ~300 lines`
> architecture.md line 151: "## Database Schema (18 models)"

Actual `prisma/schema.prisma`: **42 models, 906 lines** (`grep -c "^model " = 42`, `wc -l = 906`). The schema section lists only ~24 of the 42 models.

### 3. PRD §3.4 + architecture: coach context builder size wrong
> PRD.md line 110: "Rich context aggregation (541-line context builder pulling from all data sources)"
> architecture.md line 128: "`coach-context.ts` — 14-query context builder for AI coach"
> architecture.md line 217: "`buildCoachContext()` — 14 Prisma queries aggregating all user data"

Actual: `lib/coach-context.ts` = **1367 lines**, **22** `prisma.` call sites.

### 4. PRD §4.5 + architecture: Apple HealthKit marked "Planned (Phase 3)" but is partially shipped
> PRD.md line 187: "### 4.5 Apple HealthKit (Planned — Phase 3)"
> architecture.md line 251: "| Apple HealthKit | Native bridge | … | Planned (Phase 3) |"

Shipped: `api/healthkit-sync/route.ts`, `api/workouts/apple-watch/route.ts`; models `HealthKitSync`, `HealthKitWorkout`, `HeartRateZoneSummary`; `components/dashboard/healthkit-status.tsx`. features.md already treats HealthKit status + Apple Watch detection as live.

### 5. PRD §3.7 "V2 — Auto-sync (planned)" partially contradicted
> PRD.md lines 144–148: "**V2 — Auto-sync (planned):** Pull menstrual data from Apple HealthKit …"

HealthKit sync infrastructure now exists (see #4), so the menstrual auto-sync path is no longer purely "planned." Status should be reconciled (infra present; confirm whether menstrual ingestion is wired).

### 6. PRD §3.5 Goal types diverge from code (and from features.md)
> PRD.md line 120: "Goal types: weight, race, exam, performance, habit, custom"

`features.md` lines 203–205 and the `Goal` model use a different, larger taxonomy: types `race | strength | physique | cognitive | weight | health | custom` and subtypes including `hyrox`, `marathon`, `cfa`, `cut`, `bulk`, `recomp`, etc. PRD's list ("exam, performance, habit") is obsolete.

### 7. architecture.md API-route inventory understated for coach
> architecture.md line 107: "`coach/` — AI coach chat + sessions (2 routes)"

Actual: **3** routes — `api/coach/route.ts`, `api/coach/sessions/[id]/route.ts`, `api/coach/tradeoffs/route.ts`.

### 8. architecture.md lib/ listing omits ~20 modules
> architecture.md lines 119–132 list 14 `lib/` files.

Actual `src/lib/` has **34** files. The 14 listed all still exist (good), but see "Shipped but undocumented" for the missing ~20.

### 9. architecture.md component counts stale
> architecture.md lines 110–118 (e.g. "body/ … (6)", "dashboard/ … (7)", "mind/ … (8)")

Actual: `components/body/` = 11, `components/dashboard/` = 21, `components/mind/` = 11, plus an entire undocumented `components/mobile/` (11 files). Counts and an entire directory are missing.

### 10. PRD §3.2 / features.md "5 pre-seeded templates" vs code
> PRD.md line 64: "Experiment builder with 5 pre-seeded templates"

`lib/experiment-templates.ts` contains **6** `title:` entries. Verify whether a 6th template was added without updating the count (or whether one `title:` is a type/interface field).

### 11. PRD §4.1 Oura endpoint table omits synced data now in schema
> PRD.md lines 156–162 (Oura endpoint table) lists 5 endpoints.

`lib/sync.ts` now also persists SpO2 (`:524`), Oura workouts (`:638`), Oura sessions (`:687`), sleep-time recommendations (`:730`), resilience (`:767`), and VO2Max (`:803`) — none reflected in the PRD §4.1 table or architecture's biometric-models list. (features.md *does* document SpO2/Resilience/VO2max/bedtime as shipped, so this is a PRD+architecture gap, not a features.md gap.)

---

## Shipped but undocumented (silent features) — 13

"Undocumented" below = absent from all three source-of-truth docs unless noted. Several have their own spec files in `docs/`, but never made it into PRD/architecture/features.

### 1. Hyrox module (entire subsystem) — absent from PRD.md & architecture.md
- **lib:** `hyrox-archive.ts`, `hyrox-blocks.ts`, `hyrox-constants.ts`, `hyrox-pace.ts`, `hyrox-plan-sync.ts`, `hyrox-session-recommender.ts`, `hyrox-today.ts`
- **routes:** `api/hyrox/plan`, `api/hyrox/plan/[id]`, `api/hyrox/sessions`, `api/hyrox/today`
- **pages/components:** `app/body/hyrox/page.tsx`, `components/hyrox-summary-card.tsx`, `components/dashboard/hyrox-countdown-card.tsx`, `components/body/start-hyrox-session-button.tsx`
- **models:** `HyroxPlan`, `HyroxSession`, `HyroxStationBenchmark`
- Documented in `features.md` (`/body/hyrox`) + `docs/hyrox-module-spec.md`, but **PRD roadmap (Phase 2c) jumps straight to Arduino IMU and never mentions Hyrox**, and architecture's diagram/structure has no Hyrox node.

### 2. Meal / GI analyzer — undocumented in all three core docs
- **lib:** `meal-gi.ts`, `gi-classifier.ts`, `food-tags.ts`, `fisher.ts`, `pre-workout-fuel.ts`
- **component:** `components/mind/gi-patterns-card.tsx`
- Only spec'd in `docs/meal-gi-analyzer-spec.md`. Fisher's-exact GI-failure analysis is a substantial shipped feature with zero mention in PRD/architecture/features.

### 3. Mobile / PWA UI — entire `components/mobile/` directory undocumented
- 11 components (`mobile-dashboard`, `mobile-coach`, `mobile-tab-bar`, `mobile-training-tier`, `mobile-log-food`, etc.), plus `app/mobile.css` and `components/pwa-register.tsx`.
- No mention in architecture's component tree or PRD. (This is active work per project memory, but the docs don't acknowledge a mobile surface exists.)

### 4. Multi-user scaffolding — contradicts "single-user" architecture
- `lib/current-user.ts` (`getCurrentUserId()`), models `User` and `UserBaseline`, and `userId` scoping in newer routes (e.g. `healthkit-sync`).
- architecture.md (lines 256–263, "Security Considerations") still asserts single-user, no auth. The groundwork for multi-user has landed without an auth layer or a doc update.

### 5. Oura data-expansion models (7) — undocumented in architecture schema
`DailySpO2`, `DailyResilience`, `DailyRunningMetrics`, `DailyVO2Max`, `OuraWorkout`, `OuraSession`, `SleepTimeRecommendation`. (Surfaced in features.md prose but missing from the schema section/PRD data tables — see broken-promise #11.)

### 6. Flags system — `lib/flags.ts` + `components/mind/flags-feed.tsx`
The "this doesn't add up" self-honesty surface (contradiction/implausibility/stale-input checks). Not in any core doc.

### 7. Workout notes / discussion — `lib/workout-notes.ts`, `lib/workout-discussion.ts`
- **routes:** `api/workout-notes`, `api/workout-notes/[id]`, `api/workout-notes/[id]/analyze`
- **component:** `components/dashboard/workout-notes-block.tsx`

### 8. Life context — `api/life-context/defs`, `api/life-context/logs`
- **models:** `LifeContextDef`, `LifeContextLog`; **component:** `components/mind/life-context-card.tsx`. Undocumented.

### 9. Integrated "training call" / "today call" verdict — `lib/today-call.ts`, `lib/training-call.ts`
- **components:** `components/dashboard/today-call-card.tsx`, `today-call-hero.tsx`. The unified dashboard+/body training verdict. Undocumented.

### 10. Coach tool-use + tradeoffs — `lib/coach-tools.ts`, `api/coach/tradeoffs`
Coach has evolved into a tool-using agent with a tradeoffs endpoint. Undocumented (see broken-promise #7).

### 11. Manual workout entry — `api/workouts/manual`, `components/dashboard/manual-workout-entry.tsx`
Lets the user log workouts outside the live logger. Undocumented.

### 12. Goal↔workout tagging — model `GoalWorkoutTag`, route `api/workouts/[id]/goals`, `components/body/goal-tagger.tsx`
features.md alludes to "goal tagging at session end," but the model + dedicated route aren't in architecture's schema/route list.

### 13. Apple Watch ingestion — `api/workouts/apple-watch`, `lib/anthropic-retry.ts`
Apple Watch workout route (paired with HealthKit, see broken-promise #4) and the Anthropic retry wrapper (`anthropic-retry.ts`, used by `usda.ts` and coach) are undocumented infrastructure.

---

## Notes & non-drift confirmations
- **usda.ts is correctly described** despite its misleading filename — architecture line 128 says "Claude-powered macro estimation," and `usda.ts:1,23` indeed uses `@anthropic-ai/sdk` (`messages.create`, model `claude-sonnet-4-6`). No drift, but the filename is a trap for future readers.
- **PRD §4.2 "Model: Hardcoded (should be env variable — BUG-018)"** is still *accurate* — `usda.ts:24` hardcodes `model: "claude-sonnet-4-6"`. Doc correctly reflects the open bug.
- **Stray editor artifacts** in the tree: several `.fuse_hidden*` files under `src/app/` and `src/components/` (e.g. `src/app/body/.fuse_hidden0000000700000001`) and DB backups in `prisma/` (`dev.db.pre-hyrox.bak`, `schema.draft.prisma`, `schema.sqlite.bak`). Not doc drift, but worth gitignoring/cleaning.

---

*Generated by the `baseline-doc-drift-detector` scheduled task. Verification ran read-only against working-tree source; no code or docs were modified.*
