# Baseline — Documentation Drift Report

**Generated:** 2026-05-13 (automated scheduled task)
**Source-of-truth docs checked:** `docs/PRD.md` (v0.3, Last Updated 2026-04-06), `docs/architecture.md` (Last Updated 2026-04-06), `docs/features.md` (Last Updated 2026-04-23).
**Codebase commit state:** working tree at `/Users/kalysha/projects/baseline`, HEAD `2ebca6e` (2026-05-05).
**Prior report:** `drift-2026-04-29.md` (39 items). None of the three source-of-truth docs has been updated since that report; **every drift item from the previous run still applies.** This report re-confirms the highest-signal items and adds findings introduced by the 9 commits landed between 2026-04-29 and 2026-05-05.

---

## Summary

| Category | Count |
|---|---|
| Documented but missing or changed (broken promises) | **17** |
| Shipped but undocumented (silent features) | **22** |

Delta vs. 2026-04-29: +2 broken promises (insights p-value threshold tightened; nutrition logger sample now passes a goal-driven calorie target instead of just "balance"). +3 silent features (LifeContext-driven insight lanes, nutrition-driven insight lanes, "sometime today" timeUnknown flag).

### Critical-path callouts

- **Oura sync** (`src/lib/sync.ts`): `syncOuraData()` returns counts for **12 endpoints** (`readiness`, `sleep`, `stress`, `heartrate`, `activity`, `spo2`, `tags`, `workouts`, `sessions`, `sleepTime`, `resilience`, `vo2max`) — PRD §4.1 still lists only 5. **Unchanged from prior report; still drifted.**
- **Baseline Score calculation** (`src/lib/baseline-score.ts`): Weights 40/25/20/15, null-component redistribution, luteal `−0.4°C` (line 67) and ovulation `−0.15°C` (line 70) offsets all match PRD §3.1 and architecture data-flow exactly. **No drift.**
- **Coach context builder** (`src/lib/coach-context.ts`): file is **1,233 lines** (PRD §3.4 line 109 says 541) with **24 `prisma.*` calls** across 19+ models (architecture.md line 128 says "14-query"). Goal-lens, Hyrox integration, trade-off detection, EA/HRV-CV warning generation all shipped but unreferenced in PRD/architecture. **Unchanged from prior report; still drifted.**
- **Coach `tradeoffs` endpoint** (`/api/coach/tradeoffs/route.ts`): live, imports `hrvCV`, `energyAvailability`, `ffmFromBodyComposition` from `lib/training.ts`. Still unmentioned in PRD/architecture.
- **Cycle-phase logic**: `tempDeviationScore()` cycle-aware offsets correct. `cyclePhaseGuidance("ovulation").aclWarning = true` in `src/lib/training.ts:283` and rendered in `src/components/body/cycle-phase-guidance-card.tsx:23` (`"ACL caution"` badge). **PRD §3.3 still incorrectly lists "ACL injury risk flag during ovulation (Hewett: 3–6× higher)" as not yet shipped (line 95).**
- **Auth / OAuth flow**: `/api/auth/oura/route.ts` + `/api/auth/oura/callback/route.ts` exchange code; `getValidToken()` in `src/lib/oura.ts` handles refresh. Matches PRD §4.1 and architecture's External Integrations table. **No drift.**

---

## Documented but missing or changed

### 1. PRD §3.3 "Body Mode — Features (not yet shipped)" — all seven items are shipped (carry-over)

PRD lines 93–99 still list these as not yet shipped; all seven render in production today.

| Doc claim (PRD line) | Status | Evidence in code |
|---|---|---|
| L93 "HRV CV as overreaching signal (UI not connected)" | **Shipped** | `src/components/body/readiness-tier-card.tsx:69` renders `HRV CV: {hrvCv.toFixed(1)}%`; `src/app/body/page.tsx:393` shows "HRV CV elevated: {cv.toFixed(1)}% (Flatt threshold: 10%)". |
| L94 "Deload recommendation engine (logic exists, no UI trigger)" | **Shipped** | `src/lib/training.ts:124–151` `computeFatigueScore()` → recommendation; `src/app/body/page.tsx:360–410` renders the "Fatigue / deload signal" card and "Deload protocol" detail. |
| L95 "ACL injury risk flag during ovulation (Hewett: 3–6× higher)" | **Shipped** | `src/lib/training.ts:283` `note: "Peak power output, but 3-6x higher ACL injury risk (Hewett 2007)…"`; `src/components/body/cycle-phase-guidance-card.tsx:37–39` renders the "ACL injury risk is 3-6x higher during ovulation. 72.5% of ACL injuries…" block. |
| L96 "Volume zone display (MEV/MAV/MRV per muscle group)" | **Shipped** | `src/components/body/volume-zones.tsx:17` "Sets per muscle group vs MEV/MAV/MRV (Israetel 2021)" gauges. |
| L97 "Weekly sets per muscle group tracking" | **Shipped** | `src/app/body/page.tsx:199` `muscleSets` aggregation; rendered through `<VolumeZones>`. |
| L98 "Energy availability warning (<30 kcal/kg FFM)" | **Shipped** | `src/components/weight/tdee-card.tsx:39` `const lowEA = energyAvailability != null && energyAvailability < 30;`; warning text `tdee-card.tsx:107`. |
| L99 "Per-meal protein flag (>30g threshold)" | **Shipped** | `src/components/body/nutrition-check.tsx:82` `const excess = m.protein > 30;`; backed by `perMealProteinStatus()` in `training.ts:167`. |

### 2. PRD §7 Roadmap "Phase 2b" — two unchecked items are shipped (carry-over)

PRD line 266: `- [ ] Volume zone alerts (MEV/MAV/MRV)` — shipped (see #1).
PRD line 267: `- [ ] Deload detection UI` — shipped (see #1).

### 3. PRD §3.5 Goals — goal type taxonomy mismatch (carry-over)

PRD line 119: "Goal types: weight, race, exam, performance, habit, custom".
Actual `Goal.type` per `prisma/schema.prisma:269` comment: `race | strength | physique | cognitive | weight | health | custom`. The PRD's `exam`, `performance`, `habit` types do not exist; `strength`, `physique`, `cognitive`, `health` are missing from PRD. Subtype enum (`hyrox`, `marathon`, `5k`, etc.) is undocumented in PRD entirely.

### 4. PRD §3.4 Coach — context-builder line count and query count are stale (carry-over, worsened)

PRD line 109: "Rich context aggregation (541-line context builder pulling from all data sources)".
Actual `src/lib/coach-context.ts` = **1,233 lines** (verified `wc -l`).
architecture.md line 128 and line 217 both call it "14-query"; actual `grep -c 'prisma\.'` = **24**. Shipped surfaces unreferenced by PRD/architecture: `detectTradeoffs()`, `goalSystemPromptSection()`, `goalLenses` (relevance weights per goal type), Hyrox plan integration, pace-gap analysis.

### 5. PRD §4.1 — Oura endpoints table is incomplete (carry-over)

PRD §4.1 (lines 156–162) lists 5 endpoints; `syncOuraData()` in `src/lib/sync.ts:215–229` returns counts for 12. Missing from the PRD table: `daily_spo2`, `enhanced_tag`, `workout`, `session`, `sleep_time`, `daily_resilience`, `vo2_max`, plus the `sleep` period detail endpoint used at `sync.ts:298` to enrich `daily_sleep`.

### 6. PRD §3.7 / §4.5 — Apple HealthKit listed as Phase-3-planned but partially shipped (carry-over)

PRD line 187: "Apple HealthKit (Planned — Phase 3)". Reality: `/api/healthkit-sync/route.ts` and `/api/workouts/apple-watch/route.ts` are live; schema models `HealthKitSync`, `HealthKitWorkout`, `HeartRateZoneSummary` exist; `src/components/dashboard/healthkit-status.tsx` renders on the dashboard. Only menstrual auto-sync (PRD line 147) remains unshipped — `CyclePhaseLog.source` defaults to `"manual"` and no code path writes the `"healthkit"` value.

### 7. architecture.md §"Application Structure" — Prisma model count is stale (carry-over)

architecture.md line 76: `schema.prisma          # 18 models, ~300 lines`.
Actual: **39 models** (verified `grep -c '^model ' = 39`), **581 lines** (`wc -l`).

### 8. architecture.md §"Database Schema (18 models)" — missing models (carry-over)

The heading claims 18, the list enumerates 23, schema has 39. Models present in code but not listed in architecture.md §"Database Schema":

- Hyrox: `HyroxPlan`, `HyroxSession`, `HyroxStationBenchmark`, `GoalWorkoutTag`.
- HealthKit: `HealthKitSync`, `HealthKitWorkout`, `HeartRateZoneSummary`.
- Oura expansion: `DailySpO2`, `OuraWorkout`, `OuraSession`, `SleepTimeRecommendation`, `DailyResilience`, `DailyRunningMetrics`, `DailyVO2Max`.
- Life Context: `LifeContextDef`, `LifeContextLog` (commit `4f8302f`, 2026-05-05).

### 9. architecture.md §"Application Structure" tree — missing routes, components, lib modules (carry-over)

architecture.md lines 73–147 omit these existing files:

- Pages: `src/app/body/hyrox/page.tsx`, `src/app/mind/experiments/new/page.tsx`, `src/app/mind/experiments/[id]/page.tsx`.
- API routes: `/api/hyrox/plan`, `/api/hyrox/plan/[id]`, `/api/hyrox/today`, `/api/healthkit-sync`, `/api/workouts/apple-watch`, `/api/workouts/[id]/goals`, `/api/workouts/rpe-suggestions`, `/api/coach/tradeoffs`, `/api/coach/sessions`, `/api/coach/sessions/[id]`, `/api/life-context/defs`, `/api/life-context/logs`.
- Components: `hyrox-summary-card.tsx`, `dashboard/healthkit-status.tsx`, `dashboard/metric-card.tsx`, `mind/life-context-card.tsx`, `body/running-metrics-card.tsx`, `body/goal-tagger.tsx`, `body/nutrition-check.tsx`, `goals/countdown-ring.tsx`.
- Library files: `anthropic-retry.ts`, `utils.ts`, `hyrox-archive.ts`, `hyrox-blocks.ts`, `hyrox-constants.ts`, `hyrox-pace.ts`, `hyrox-plan-sync.ts`, `hyrox-session-recommender.ts`.

### 10. architecture.md §"Application Structure" — annotation counts stale (carry-over)

Annotations like "OAuth2 flow (2 routes)", "Workout CRUD + sets + trends (7 routes)" no longer reflect actual counts. Workouts directory currently has 7 distinct handlers (`route.ts`, `[id]/route.ts`, `[id]/sets/route.ts`, `[id]/sets/[setId]/route.ts`, `[id]/goals/route.ts`, `apple-watch/route.ts`, `rpe-suggestions/route.ts`, `trends/route.ts`) — i.e. 8.

### 11. architecture.md `lib/` annotation for coach-context (carry-over)

architecture.md line 128: `coach-context.ts      # 14-query context builder for AI coach` — see #4. Actual 24 queries plus goal-lens system.

### 12. architecture.md §"Data Flow — Coach Chat" — stale query count (carry-over)

architecture.md line 217: "buildCoachContext() — 14 Prisma queries aggregating all user data". Actual 24 (see #4).

### 13. architecture.md §"Application Structure" — file counts in component descriptions stale (carry-over)

architecture.md lines 111–116:

- "components/dashboard/ … (7)" — actual 9 files (`activity-card`, `baseline-score-card`, `calorie-balance-card`, `cycle-phase-selector`, `healthkit-status`, `macro-summary`, `metric-card`, `sync-button`, `trend-chart`).
- "components/mind/ … (8)" — actual 9 (adds `life-context-card.tsx`).
- "components/body/ … (6)" — actual 8 (adds `goal-tagger.tsx`, `nutrition-check.tsx`, `running-metrics-card.tsx`).
- "components/coach/ … (1)" — correct.
- "components/goals/ … (1)" — actual 2 (`goals-manager.tsx`, `countdown-ring.tsx`).
- "components/weight/ … (5)" — correct.

### 14. PRD §3.7 V2 Auto-sync — Oura body-temp proxy never implemented (carry-over)

PRD line 148 names "Oura body temperature deviation as proxy signal for phase detection". No code path consumes `temperatureDeviation` for phase detection — only the Baseline Score consumes it. **Unchanged promise**; flagged each cycle.

### 15. features.md citation paths reference `research/` while PRD references top-level docs (carry-over)

features.md lines 4 and 228 reference `research/body-mode-research.md` and `research/variable-research.md`. PRD line 295 still cites `body-mode-research.md` as a docs-relative path. Actual location: `research/body-mode-research.md`.

### 16. **NEW** — PRD §3.2 Passive insights p-value threshold has tightened

PRD line 70: "Passive insights: auto-correlates tags with biometric outcomes (90-day rolling, **p < 0.15** filter)".
Actual `src/lib/insights.ts:133`: `if (pValue >= 0.10) continue;` — i.e. **p < 0.10**. Tightened sometime before commit `7a6b3b4` (2026-05-05, "refactor(insights)…"). PRD figure is now misleadingly loose.

### 17. **NEW** — PRD §3.1 "Calorie balance card" description is incomplete

PRD line 53: "Calorie balance card (intake from nutrition logger vs burn from Oura)".
Actual `src/components/dashboard/calorie-balance-card.tsx` now takes `goal` and `goalCals` props (commit `cbf7199`, 2026-05-05, "refactor(dashboard): goal-driven calorie targets") and renders goal-driven classifications such as `"In target deficit range"`, `"Surplus — goal is cut"`, `"Near maintenance"`. The card is no longer a pure in-vs-out balance; PRD undersells it.

---

## Shipped but undocumented

### A. Entire Hyrox race-prep module (carry-over)

Schema: `HyroxPlan`, `HyroxSession`, `HyroxStationBenchmark`. API: `/api/hyrox/plan`, `/api/hyrox/plan/[id]`, `/api/hyrox/today`. UI: `src/app/body/hyrox/page.tsx`, `src/components/hyrox-summary-card.tsx`. Library: `hyrox-archive.ts`, `hyrox-blocks.ts`, `hyrox-constants.ts`, `hyrox-pace.ts`, `hyrox-plan-sync.ts`, `hyrox-session-recommender.ts`. Coach context integrates Hyrox plan + pace-gap. Covered by `features.md` and a separate `docs/hyrox-module-spec.md` but **PRD.md and architecture.md never mention Hyrox at all** and PRD §7 Roadmap doesn't reference it.

### B. Entire Life Context module (carry-over, now expanded)

Schema: `LifeContextDef`, `LifeContextLog` (`prisma/schema.prisma:553–581`). API: `/api/life-context/defs`, `/api/life-context/logs`. UI: `src/components/mind/life-context-card.tsx`. Now also: insights engine consumes `LifeContextLog` (`src/lib/insights.ts:173–177`) so day-flags become surfaced correlations alongside tags. **Not referenced in PRD, architecture, or features.md.**

### C. Apple HealthKit ingest pipeline (carry-over)

See PRD §4.5 discrepancy in #6. Schema `HealthKitSync` / `HealthKitWorkout` / `HeartRateZoneSummary`; API `/api/healthkit-sync`, `/api/workouts/apple-watch`; UI `dashboard/healthkit-status.tsx`. Spec lives at `docs/healthkit-sync-spec.md` but PRD §4.5 still calls it "Planned — Phase 3".

### D. Coach trade-off detection endpoint (carry-over)

`/api/coach/tradeoffs/route.ts` plus `detectTradeoffs()` in `coach-context.ts`. Generates warnings like the EA-while-cutting block at `coach-context.ts:246–252`. Not in PRD or architecture.

### E. Goal-Workout tagging system (carry-over)

Schema `GoalWorkoutTag`; API `/api/workouts/[id]/goals`; UI `src/components/body/goal-tagger.tsx`. Mentioned in features.md `/body/workout/[id]` but absent from PRD/architecture.

### F. Daily Running Metrics ingestion + display (carry-over)

Schema `DailyRunningMetrics` (GCT, vertical oscillation, stride length, cardio recovery, etc.). UI `src/components/body/running-metrics-card.tsx`. In features.md, absent from PRD/architecture.

### G. VO2 Max tracking (carry-over)

Schema `DailyVO2Max`. Display logic in `/body/page.tsx` and `body/hyrox/page.tsx`. `sync.ts` returns a `vo2max` count. In features.md, absent from PRD/architecture.

### H. Daily Resilience metric (carry-over)

Schema `DailyResilience`. Rendered as one of the six dashboard metric cards (`src/app/page.tsx:324–336`). In features.md, missing from PRD §3.1 metric list.

### I. SpO2 metric card (carry-over)

Schema `DailySpO2`. Rendered as dashboard metric card (`src/app/page.tsx:314–323`). In features.md, missing from PRD §4.1 Oura endpoint table.

### J. Bedtime recommendation card (carry-over)

Schema `SleepTimeRecommendation`. Read in `src/app/page.tsx` and `src/lib/coach-context.ts`. In features.md ("Bedtime recommendation"), absent from PRD/architecture.

### K. Today's sessions card — Oura breathing/meditation/nap (carry-over)

Schema `OuraSession`. Rendered at `src/app/page.tsx:381–402`. In features.md, absent from PRD/architecture.

### L. Oura enhanced_tag ingestion (carry-over)

`sync.ts` calls `ouraFetch<…>("enhanced_tag", …)`; `ActivityTag.ouraTagId` field dedupes them. Not in PRD §4.1 or architecture.

### M. Oura workout records (carry-over)

Schema `OuraWorkout`; pulled by `sync.ts`. Not in docs.

### N. Goal-Coach lens / primary-goal frame system (carry-over)

Large feature in `coach-context.ts` (`goalLenses` table, `sectionOrder`, `coachingFrame`, `goalSystemPromptSection()` lines 317–355). Documented in `docs/goal-coach-redesign-spec.md` and referenced in features.md but **absent from PRD §3.4 and architecture.md**.

### O. `anthropic-retry.ts` library (carry-over)

Wrapper for Claude API with retry logic at `src/lib/anthropic-retry.ts`. Architecture's `lib/` listing omits it.

### P. `utils.ts` library (carry-over)

Shared helpers at `src/lib/utils.ts`. Architecture's `lib/` listing omits it.

### Q. `/api/profile` endpoint (carry-over)

Listed in architecture but undescribed; PRD has no mention. Underpins UserProfile sex/age/height/etc. used by TDEE math.

### R. Workout `rpe-suggestions` endpoint (carry-over)

PRD §3.3 line 87 mentions "RPE suggestions API" but architecture's route list never enumerates `/api/workouts/rpe-suggestions/route.ts`.

### S. Set-level mutation route (carry-over)

`/api/workouts/[id]/sets/[setId]/route.ts` exists for individual set update/delete; architecture only mentions the collection-level `/sets` endpoint.

### T. **NEW** — Insights "nutrition & life-context lanes"

Commit `7a6b3b4` (2026-05-05) added nutrition and life-context lanes to passive insights. `src/lib/insights.ts:168–191` joins `nutritionLog`, `nutritionEntry`, and `lifeContextLog` into the correlation engine alongside `activityTag`. Output is grouped multi-metric findings (`Insight` type now carries an `InsightMetric[]` array with `pValue` per metric). PRD §3.2 still describes insights as "auto-correlates tags with biometric outcomes" — a single-source framing that no longer reflects the engine.

### U. **NEW** — "Sometime today" `timeUnknown` flag for tags and nutrition

Commit `02ae462` (2026-05-05). Schema `NutritionEntry.timeUnknown` (`prisma/schema.prisma:178`) and tag-side equivalent flow through `src/components/mind/nutrition-input.tsx`, `mind/quick-tag.tsx`, `mind/tag-timeline.tsx`, `mind/nutrition-log.tsx`, and the `/api/nutrition` route. features.md does allude to "an unknown-time flag when you forget" but PRD/architecture do not mention the field or its impact on per-meal protein and timestamping flows.

### V. **NEW** — Dashboard goal-driven calorie restructure

Commit `cbf7199` (2026-05-05) reshaped the Weight + TDEE + CalorieBalance section of the dashboard. `goalCals = goalCalories(tdee, goal, weeklyRateLb)` is computed in `src/app/page.tsx:200` and threaded into `WeightCard`, `TdeeCard`, and `CalorieBalanceCard`. PRD §3.1 still describes only "intake vs burn" and a separate "weight card"; the integrated goal flow is undocumented.

---

## Recommendations (prioritized — unchanged categories, re-confirmed)

1. **Edit PRD §3.3 "Features (not yet shipped)" list** — all seven items must move into "shipped". This list is the single most misleading section of the PRD and has been wrong for at least 2 weeks.
2. **Edit PRD §4.1 Oura endpoints table** to include the 7 additional endpoints (`daily_spo2`, `enhanced_tag`, `workout`, `session`, `sleep_time`, `daily_resilience`, `vo2_max`), plus call out that `sleep` period detail is fetched alongside `daily_sleep`.
3. **Edit PRD §4.5 / §7** to reflect that Apple HealthKit workouts + HR-zones ingestion ships today; only menstrual-data auto-sync remains planned.
4. **Add Hyrox Mode section to PRD §3** and a Hyrox subsystem block to the architecture diagram + schema list.
5. **Add Life Context to PRD §3 and architecture.md.** Currently zero coverage in either, despite being a wired-up Mind Mode surface and a contributor to passive insights.
6. **Refresh architecture.md model count and tree** — "18 models / ~300 lines" → "39 models / 581 lines" and regenerate the schema enumeration (consider auto-generating from Prisma).
7. **Sync goal-type taxonomy** in PRD §3.5 with the schema/UI (`race | strength | physique | cognitive | weight | health | custom`).
8. **Refresh coach-context line count and query count** in PRD §3.4 (1,233 lines, ~24 queries) and architecture.md "Coach Chat" data-flow.
9. **Update PRD §3.2** to reflect the multi-lane insights engine (tag + nutrition + life-context) and the tightened p-value cutoff (`p < 0.10`).
10. **Update PRD §3.1** to describe the goal-driven CalorieBalance/TDEE/Weight card cluster, the 6-card metric grid (Readiness, Sleep, HRV, Stress, SpO2, Resilience), and HealthKit status / Bedtime / Sessions cards.

---

*Auto-generated drift report. To re-run, trigger the `baseline-doc-drift-detector` scheduled task. Prior report: `drift-2026-04-29.md`.*
