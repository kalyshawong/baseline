# Baseline — Documentation Drift Report

**Generated:** 2026-05-27 (automated scheduled task)
**Source-of-truth docs checked:** `docs/PRD.md` (v0.3, Last Updated 2026-04-06), `docs/architecture.md` (Last Updated 2026-04-06), `docs/features.md` (Last Updated 2026-04-23).
**Codebase commit state:** working tree at `/Users/kalysha/projects/baseline`, HEAD `2ebca6e` (2026-05-05). **HEAD unchanged since the prior report.**
**Prior report:** `drift-2026-05-13.md` (17 broken promises + 22 silent features).

---

## Summary

| Category | Count | Δ vs. 2026-05-13 |
|---|---|---|
| Documented but missing or changed (broken promises) | **17** | unchanged |
| Shipped but undocumented (silent features) | **23** | +1 (uncommitted backfill workflow) |

No source-of-truth doc has been edited since the 2026-05-13 report. No new commits have landed on `main`. The drift situation is therefore essentially identical to the last run — re-confirming the prior report rather than introducing new findings, with one exception: a 5-file *uncommitted* "log past workout" workflow now lives in the working tree, surfacing on `/body` but absent from every doc.

### Critical-path callouts (re-verified today)

- **Oura sync** (`src/lib/sync.ts`, 769 lines): `syncOuraData()` returns counts for 12 endpoints (`readiness`, `sleep`, `stress`, `heartrate`, `activity`, `spo2`, `tags`, `workouts`, `sessions`, `sleepTime`, `resilience`, `vo2max`). PRD §4.1 (lines 156–162) still lists only 5. **Still drifted.**
- **Baseline Score calculation** (`src/lib/baseline-score.ts`): weights 40/25/20/15 confirmed (lines 94–98), null-component redistribution confirmed (lines 100–127), luteal `−0.4°C` at line 67, ovulation `−0.15°C` at line 70, green/yellow/red cutoffs at 80/60 (lines 140–149). All match PRD §3.1 and architecture data-flow exactly. **No drift.**
- **Coach context builder** (`src/lib/coach-context.ts`): **1,233 lines** (PRD §3.4 line 109 still says "541-line"), **24 `prisma.*` calls** (architecture.md line 128 and line 217 both still say "14-query"). **Still drifted.**
- **Cycle-phase logic**: `cyclePhaseGuidance("ovulation").aclWarning = true` in `src/lib/training.ts:283` (`"Peak power output, but 3-6x higher ACL injury risk (Hewett 2007)…"`); luteal/ovulation temp offsets correct in `baseline-score.ts`. PRD §3.3 line 95 still incorrectly lists the ACL flag as not yet shipped. **Still drifted.**
- **Auth / OAuth flow**: `/api/auth/oura/route.ts` + `/api/auth/oura/callback/route.ts` present and unchanged; `getValidToken()` lives in `src/lib/oura.ts` (123 lines). Matches PRD §4.1 + architecture's External Integrations table. **No drift.**

---

## Documented but missing or changed

*All 17 items below carry over from `drift-2026-05-13.md`. None has been remediated. Re-verified against current `HEAD` + source docs today.*

### 1. PRD §3.3 "Body Mode — Features (not yet shipped)" — all seven items are shipped

PRD lines 93–99 still list these as not yet shipped. All seven render in production today.

| Doc claim (PRD line) | Status | Evidence in code |
|---|---|---|
| L93 "HRV CV as overreaching signal (UI not connected)" | **Shipped** | `src/components/body/readiness-tier-card.tsx:69` renders `HRV CV: {hrvCv.toFixed(1)}%`; `readiness-tier-card.tsx:79` shows the Flatt-threshold warning when `hrvCv > 10`. |
| L94 "Deload recommendation engine (logic exists, no UI trigger)" | **Shipped** | `src/lib/training.ts:124–156` `computeFatigueScore()` → `recommendation`; rendered at `src/app/body/page.tsx:270` (`computeFatigueScore({...})`) and 360–410 ("Fatigue / deload signal" card; the `weeksSinceDeload >= 5` line at `body/page.tsx:386` cites Pritchard directly). |
| L95 "ACL injury risk flag during ovulation (Hewett: 3–6× higher)" | **Shipped** | `src/lib/training.ts:283` `note: "Peak power output, but 3-6x higher ACL injury risk (Hewett 2007). Extra warm-up, controlled landings, knee sleeves for heavy squats."`; `cyclePhaseGuidance(...).aclWarning = true`; consumed by `src/components/body/cycle-phase-guidance-card.tsx`. |
| L96 "Volume zone display (MEV/MAV/MRV per muscle group)" | **Shipped** | `src/components/body/volume-zones.tsx:17` "Sets per muscle group vs MEV/MAV/MRV (Israetel 2021)" with full gauge UI; backed by `volumeZones` table in `training.ts:29–40`. |
| L97 "Weekly sets per muscle group tracking" | **Shipped** | `src/app/body/page.tsx` aggregates `muscleSets` and threads to `<VolumeZones>`. |
| L98 "Energy availability warning (<30 kcal/kg FFM)" | **Shipped** | `src/components/weight/tdee-card.tsx:39` `const lowEA = energyAvailability != null && energyAvailability < 30;` rendered in red panel at lines 102–111. **Also** shipped in `src/components/body/nutrition-check.tsx:23` + 103–111 ("Loucks 2011" cited inline). |
| L99 "Per-meal protein flag (>30g threshold)" | **Shipped** | `src/components/body/nutrition-check.tsx:82` `const excess = m.protein > 30;`; backed by `perMealProteinStatus()` in `training.ts:167–172` (returns `"high"` when grams > 30). |

### 2. PRD §7 Roadmap "Phase 2b" — two unchecked items are shipped

PRD line 266: `- [ ] Volume zone alerts (MEV/MAV/MRV)` — shipped (see #1).
PRD line 267: `- [ ] Deload detection UI` — shipped (see #1).

### 3. PRD §3.5 Goals — goal type taxonomy mismatch

PRD line 119: "Goal types: weight, race, exam, performance, habit, custom".
Actual `Goal.type` per `prisma/schema.prisma:269` comment: `race | strength | physique | cognitive | weight | health | custom`. The PRD's `exam`, `performance`, `habit` types do not exist; `strength`, `physique`, `cognitive`, `health` are missing from PRD. The full subtype enum (`hyrox`, `marathon`, `5k`, `10k`, `triathlon`, `powerlifting_meet`, `bodybuilding`, `cfa`, `finals`, `certification`, `cut`, `bulk`, `recomp`, `maintain`, `sleep_optimization`, `hrv_baseline`, `stress_management`, `custom`) is undocumented in PRD entirely.

### 4. PRD §3.4 Coach — context-builder line/query counts are stale

PRD line 109: "Rich context aggregation (541-line context builder pulling from all data sources)". Actual `src/lib/coach-context.ts` = **1,233 lines** (`wc -l`). architecture.md line 128 and line 217 both call it "14-query"; actual `grep -c 'prisma\.'` = **24**. Shipped surfaces unreferenced by PRD/architecture: `detectTradeoffs()`, `goalSystemPromptSection()`, `goalLenses` (relevance weights per goal type), Hyrox plan integration, pace-gap analysis.

### 5. PRD §4.1 — Oura endpoints table is incomplete

PRD §4.1 (lines 156–162) lists 5 endpoints. `syncOuraData()` in `src/lib/sync.ts` returns counts for 12. Missing from the PRD table: `daily_spo2`, `enhanced_tag`, `workout`, `session`, `sleep_time`, `daily_resilience`, `vo2_max`, plus the `sleep` period detail endpoint used at `sync.ts` to enrich `daily_sleep`.

### 6. PRD §3.7 / §4.5 — Apple HealthKit listed as Phase-3-planned but partially shipped

PRD line 187: "Apple HealthKit (Planned — Phase 3)". Reality: `/api/healthkit-sync/route.ts` and `/api/workouts/apple-watch/route.ts` are live; schema models `HealthKitSync`, `HealthKitWorkout`, `HeartRateZoneSummary` exist; `src/components/dashboard/healthkit-status.tsx` renders on the dashboard. Only menstrual auto-sync (PRD line 147) remains unshipped — `CyclePhaseLog.source` defaults to `"manual"` and no code path writes the `"healthkit"` value.

### 7. architecture.md §"Application Structure" — Prisma model count is stale

architecture.md line 76: `schema.prisma          # 18 models, ~300 lines`.
Actual: **39 models** (verified `grep -c '^model '`), **581 lines** (`wc -l`).

### 8. architecture.md §"Database Schema (18 models)" — missing models

The heading claims 18, the list enumerates 23, schema has 39. Models present in code but missing from architecture.md §"Database Schema":

- Hyrox: `HyroxPlan`, `HyroxSession`, `HyroxStationBenchmark`, `GoalWorkoutTag`.
- HealthKit: `HealthKitSync`, `HealthKitWorkout`, `HeartRateZoneSummary`.
- Oura expansion: `DailySpO2`, `OuraWorkout`, `OuraSession`, `SleepTimeRecommendation`, `DailyResilience`, `DailyRunningMetrics`, `DailyVO2Max`.
- Life Context: `LifeContextDef`, `LifeContextLog`.

### 9. architecture.md §"Application Structure" tree — missing routes, components, lib modules

architecture.md lines 73–147 omit these existing files:

- Pages: `src/app/body/hyrox/page.tsx`, `src/app/mind/experiments/new/page.tsx`, `src/app/mind/experiments/[id]/page.tsx`.
- API routes: `/api/hyrox/plan`, `/api/hyrox/plan/[id]`, `/api/hyrox/today`, `/api/healthkit-sync`, `/api/workouts/apple-watch`, `/api/workouts/[id]/goals`, `/api/workouts/rpe-suggestions`, `/api/coach/tradeoffs`, `/api/coach/sessions`, `/api/coach/sessions/[id]`, `/api/life-context/defs`, `/api/life-context/logs`.
- Components: `hyrox-summary-card.tsx`, `dashboard/healthkit-status.tsx`, `dashboard/metric-card.tsx`, `mind/life-context-card.tsx`, `body/running-metrics-card.tsx`, `body/goal-tagger.tsx`, `body/nutrition-check.tsx`, `goals/countdown-ring.tsx`.
- Library files: `anthropic-retry.ts`, `utils.ts`, `hyrox-archive.ts`, `hyrox-blocks.ts`, `hyrox-constants.ts`, `hyrox-pace.ts`, `hyrox-plan-sync.ts`, `hyrox-session-recommender.ts`.

### 10. architecture.md §"Application Structure" — annotation counts stale

Annotations like "OAuth2 flow (2 routes)", "Workout CRUD + sets + trends (7 routes)" no longer reflect actual counts. Workouts directory currently has 8 distinct handlers (`route.ts`, `[id]/route.ts`, `[id]/sets/route.ts`, `[id]/sets/[setId]/route.ts`, `[id]/goals/route.ts`, `apple-watch/route.ts`, `rpe-suggestions/route.ts`, `trends/route.ts`).

### 11. architecture.md `lib/` annotation for coach-context

architecture.md line 128: `coach-context.ts      # 14-query context builder for AI coach` — see #4. Actual 24 queries plus goal-lens system.

### 12. architecture.md §"Data Flow — Coach Chat" — stale query count

architecture.md line 217: "buildCoachContext() — 14 Prisma queries aggregating all user data". Actual 24 (see #4).

### 13. architecture.md §"Application Structure" — file counts in component descriptions stale

architecture.md lines 111–116:

- "components/dashboard/ … (7)" — actual **9** files (`activity-card`, `baseline-score-card`, `calorie-balance-card`, `cycle-phase-selector`, `healthkit-status`, `macro-summary`, `metric-card`, `sync-button`, `trend-chart`).
- "components/mind/ … (8)" — actual **9** (adds `life-context-card.tsx`).
- "components/body/ … (6)" — actual **8** (adds `goal-tagger.tsx`, `nutrition-check.tsx`, `running-metrics-card.tsx`).
- "components/coach/ … (1)" — correct.
- "components/goals/ … (1)" — actual **2** (`goals-manager.tsx`, `countdown-ring.tsx`).
- "components/weight/ … (5)" — correct.

### 14. PRD §3.7 V2 Auto-sync — Oura body-temp proxy never implemented

PRD line 148 names "Oura body temperature deviation as proxy signal for phase detection". No code path consumes `temperatureDeviation` for phase detection — only the Baseline Score consumes it. **Unchanged promise.**

### 15. features.md citation paths reference `research/` while PRD references top-level docs

features.md lines 4 and 228 reference `research/body-mode-research.md` and `research/variable-research.md`. PRD line 295 still cites `body-mode-research.md` as a docs-relative path. Actual location is in the `research/` subdirectory.

### 16. PRD §3.2 Passive insights p-value threshold has tightened

PRD line 70: "Passive insights: auto-correlates tags with biometric outcomes (90-day rolling, **p < 0.15** filter)".
Actual `src/lib/insights.ts:133`: `if (pValue >= 0.10) continue;` — i.e. **p < 0.10**. PRD figure is now misleadingly loose.

### 17. PRD §3.1 "Calorie balance card" description is incomplete

PRD line 53: "Calorie balance card (intake from nutrition logger vs burn from Oura)". Actual `src/components/dashboard/calorie-balance-card.tsx` now takes `goal` and `goalCals` props (commit `cbf7199`, 2026-05-05) and renders goal-driven classifications such as `"In target deficit range"`, `"Surplus — goal is cut"`, `"Near maintenance"`. The card is no longer a pure in-vs-out balance.

---

## Shipped but undocumented

*Items A–V carry over from `drift-2026-05-13.md`; item W is **new** to this report (uncommitted working-tree work).*

### A. Entire Hyrox race-prep module (carry-over)

Schema: `HyroxPlan`, `HyroxSession`, `HyroxStationBenchmark`. API: `/api/hyrox/plan`, `/api/hyrox/plan/[id]`, `/api/hyrox/today`. UI: `src/app/body/hyrox/page.tsx`, `src/components/hyrox-summary-card.tsx`. Library: `hyrox-archive.ts`, `hyrox-blocks.ts`, `hyrox-constants.ts`, `hyrox-pace.ts`, `hyrox-plan-sync.ts`, `hyrox-session-recommender.ts`. Coach context integrates Hyrox plan + pace-gap. Covered by `features.md` and the separate `docs/hyrox-module-spec.md`, but **PRD.md and architecture.md never mention Hyrox at all** and PRD §7 Roadmap doesn't reference it.

### B. Entire Life Context module (carry-over)

Schema: `LifeContextDef`, `LifeContextLog` (`prisma/schema.prisma:553–581`). API: `/api/life-context/defs`, `/api/life-context/logs`. UI: `src/components/mind/life-context-card.tsx`. Insights engine consumes `LifeContextLog` so day-flags become surfaced correlations alongside tags. **Not referenced in PRD, architecture, or features.md.**

### C. Apple HealthKit ingest pipeline (carry-over)

See PRD §4.5 discrepancy in #6. Schema `HealthKitSync` / `HealthKitWorkout` / `HeartRateZoneSummary`; API `/api/healthkit-sync`, `/api/workouts/apple-watch`; UI `dashboard/healthkit-status.tsx`. Spec lives at `docs/healthkit-sync-spec.md` but PRD §4.5 still calls it "Planned — Phase 3".

### D. Coach trade-off detection endpoint (carry-over)

`/api/coach/tradeoffs/route.ts` plus `detectTradeoffs()` in `coach-context.ts`. Generates warnings like the EA-while-cutting block. Not in PRD or architecture.

### E. Goal-Workout tagging system (carry-over)

Schema `GoalWorkoutTag`; API `/api/workouts/[id]/goals`; UI `src/components/body/goal-tagger.tsx`. Mentioned in features.md `/body/workout/[id]` but absent from PRD/architecture.

### F. Daily Running Metrics ingestion + display (carry-over)

Schema `DailyRunningMetrics` (GCT, vertical oscillation, stride length, cardio recovery, etc.). UI `src/components/body/running-metrics-card.tsx`. In features.md, absent from PRD/architecture.

### G. VO2 Max tracking (carry-over)

Schema `DailyVO2Max`. Display logic in `/body/page.tsx` and `body/hyrox/page.tsx`. `sync.ts` returns a `vo2max` count. In features.md, absent from PRD/architecture.

### H. Daily Resilience metric (carry-over)

Schema `DailyResilience`. Rendered as one of the six dashboard metric cards. In features.md, missing from PRD §3.1 metric list.

### I. SpO2 metric card (carry-over)

Schema `DailySpO2`. Rendered as dashboard metric card. In features.md, missing from PRD §4.1 Oura endpoint table.

### J. Bedtime recommendation card (carry-over)

Schema `SleepTimeRecommendation`. Read in `src/app/page.tsx` and `src/lib/coach-context.ts`. In features.md ("Bedtime recommendation"), absent from PRD/architecture.

### K. Today's sessions card — Oura breathing/meditation/nap (carry-over)

Schema `OuraSession`. Rendered on the dashboard. In features.md, absent from PRD/architecture.

### L. Oura enhanced_tag ingestion (carry-over)

`sync.ts` calls `ouraFetch<…>("enhanced_tag", …)`; `ActivityTag.ouraTagId` field dedupes them. Not in PRD §4.1 or architecture.

### M. Oura workout records (carry-over)

Schema `OuraWorkout`; pulled by `sync.ts`. Not in docs.

### N. Goal-Coach lens / primary-goal frame system (carry-over)

Large feature in `coach-context.ts` (`goalLenses` table, `sectionOrder`, `coachingFrame`, `goalSystemPromptSection()`). Documented in `docs/goal-coach-redesign-spec.md` and referenced in features.md but **absent from PRD §3.4 and architecture.md**.

### O. `anthropic-retry.ts` library (carry-over)

Wrapper for Claude API with retry logic at `src/lib/anthropic-retry.ts`. Architecture's `lib/` listing omits it.

### P. `utils.ts` library (carry-over)

Shared helpers at `src/lib/utils.ts`. Architecture's `lib/` listing omits it.

### Q. `/api/profile` endpoint (carry-over)

Listed in architecture but undescribed; PRD has no mention. Underpins UserProfile sex/age/height/etc. used by TDEE math.

### R. Workout `rpe-suggestions` endpoint (carry-over)

PRD §3.3 line 87 mentions "RPE suggestions API" but architecture's route list never enumerates `/api/workouts/rpe-suggestions/route.ts`.

### S. Set-level mutation route (carry-over)

`/api/workouts/[id]/sets/[setId]/route.ts` exists for individual set update/delete; architecture only mentions the collection-level `/sets` endpoint.

### T. Insights "nutrition & life-context lanes" (carry-over)

`src/lib/insights.ts:168–191` joins `nutritionLog`, `nutritionEntry`, and `lifeContextLog` into the correlation engine alongside `activityTag`. Output is grouped multi-metric findings (`Insight` type now carries an `InsightMetric[]` array). PRD §3.2 still describes insights as "auto-correlates tags with biometric outcomes" — a single-source framing that no longer reflects the engine.

### U. "Sometime today" `timeUnknown` flag for tags and nutrition (carry-over)

Schema `NutritionEntry.timeUnknown` (`prisma/schema.prisma:178`) and tag-side equivalent flow through `mind/nutrition-input.tsx`, `mind/quick-tag.tsx`, `mind/tag-timeline.tsx`, `mind/nutrition-log.tsx`, and the `/api/nutrition` route. features.md does allude to "an unknown-time flag when you forget" but PRD/architecture do not mention the field or its impact on per-meal protein and timestamping flows.

### V. Dashboard goal-driven calorie restructure (carry-over)

Commit `cbf7199` reshaped the Weight + TDEE + CalorieBalance section. `goalCals = goalCalories(tdee, goal, weeklyRateLb)` is computed in `src/app/page.tsx` and threaded into `WeightCard`, `TdeeCard`, and `CalorieBalanceCard`. PRD §3.1 still describes only "intake vs burn" and a separate "weight card"; the integrated goal flow is undocumented.

### W. **NEW** — "Log past workout" / backfill workflow (uncommitted)

`git status` shows 5 modified files implementing a backfill workflow that surfaces on `/body` today but is **uncommitted** and not in any doc:

- `src/app/api/workouts/route.ts` (+13/-3): `POST /api/workouts` now accepts an optional `date` field. When supplied, the new session's `startedAt` is anchored to noon of that date (`new Date(date + "T12:00:00.000Z")`) rather than `new Date()`. Snapshot readiness and cycle phase are fetched for the historical date.
- `src/app/body/page.tsx` (+15): adds a `+ Add Workout` CTA at the top of the Strength Training section, plus a `Log past workout` text link that navigates to `/body/workout/new?backfill=1`.
- `src/app/body/workout/new/page.tsx` (+33/-2): adds a date-picker (`max={todayStr}`) that defaults to today and reads `?backfill=1` to auto-focus; if the selected date differs from today, the POST payload carries `date` to trigger the backfill code path.
- `src/app/body/workout/[id]/page.tsx` (+6): computes `isBackfill = sessionDay < todayDay` (UTC day comparison) and forwards it to `<WorkoutLogger>`.
- `src/components/body/workout-logger.tsx` (+8/-4): receives `isBackfill?: boolean`. When true, the rest timer is suppressed (`if (!isBackfill)` around `setTimerRunning(true)`; `{!isBackfill && timerRunning && ...}` around the rest-timer card).

Net effect: a meaningful new user-facing workflow (and a new API contract) lives in the working tree, but no source doc — PRD §3.3, features.md `/body/workout/new`, features.md `/body/workout/[id]`, or architecture.md — mentions it. **Recommend either committing + documenting or reverting before the next drift run.**

---

## Recommendations (prioritized — unchanged categories, re-confirmed)

1. **Edit PRD §3.3 "Features (not yet shipped)" list** — all seven items must move into "shipped". This list is the single most misleading section of the PRD and has been wrong for at least 4 weeks (flagged in 2026-04-29, 2026-05-13, and now 2026-05-27 reports).
2. **Edit PRD §4.1 Oura endpoints table** to include the 7 additional endpoints (`daily_spo2`, `enhanced_tag`, `workout`, `session`, `sleep_time`, `daily_resilience`, `vo2_max`), plus call out that the `sleep` period detail is fetched alongside `daily_sleep`.
3. **Edit PRD §4.5 / §7** to reflect that Apple HealthKit workouts + HR-zones ingestion ships today; only menstrual-data auto-sync remains planned.
4. **Add Hyrox Mode section to PRD §3** and a Hyrox subsystem block to the architecture diagram + schema list.
5. **Add Life Context to PRD §3 and architecture.md.** Currently zero coverage in either, despite being a wired-up Mind Mode surface and a contributor to passive insights.
6. **Refresh architecture.md model count and tree** — "18 models / ~300 lines" → "39 models / 581 lines" and regenerate the schema enumeration (consider auto-generating from Prisma).
7. **Sync goal-type taxonomy** in PRD §3.5 with the schema/UI (`race | strength | physique | cognitive | weight | health | custom`).
8. **Refresh coach-context line count and query count** in PRD §3.4 (1,233 lines, ~24 queries) and architecture.md "Coach Chat" data-flow.
9. **Update PRD §3.2** to reflect the multi-lane insights engine (tag + nutrition + life-context) and the tightened p-value cutoff (`p < 0.10`).
10. **Update PRD §3.1** to describe the goal-driven CalorieBalance/TDEE/Weight card cluster, the 6-card metric grid (Readiness, Sleep, HRV, Stress, SpO2, Resilience), and HealthKit status / Bedtime / Sessions cards.
11. **Resolve the uncommitted backfill workflow (item W).** Either ship it (and document in features.md `/body/workout/new` + PRD §3.3) or revert. Letting a documented user-facing CTA float as an uncommitted change is itself a drift risk.

---

*Auto-generated drift report. To re-run, trigger the `baseline-doc-drift-detector` scheduled task. Prior report: `drift-2026-05-13.md`.*
