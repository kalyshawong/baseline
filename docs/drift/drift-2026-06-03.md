# Baseline — Documentation Drift Report

**Generated:** 2026-06-03 (automated scheduled task)
**Source-of-truth docs checked:** `docs/PRD.md` (v0.3, Last Updated 2026-04-06), `docs/architecture.md` (Last Updated 2026-04-06), `docs/features.md` (Last Updated 2026-04-23).
**Codebase state:** working tree at `/Users/kalysha/projects/baseline`, HEAD `80a25d8` (2026-05-31, *"Athletic redesign — Body, Mind, Coach screens + training call colors"*), **plus 10 uncommitted modified files** in the working tree.
**Prior report:** `drift-2026-05-27.md` (17 broken promises + 23 silent features), which was taken against HEAD `2ebca6e` (2026-05-05).

---

## Summary

| Category | Count | Δ vs. 2026-05-27 |
|---|---|---|
| Documented but missing or changed (broken promises) | **18** | +1 |
| Shipped but undocumented (silent features) | **27** | +4 |

**The drift is now wider than at the last run.** Between the prior report and today, HEAD advanced six commits (`2ebca6e` → `80a25d8`, dated 2026-05-29 through 2026-05-31), landing an "Athletic redesign" of the Body/Mind/Coach/Goals/Dashboard screens. None of the three source-of-truth docs has been edited since `ac1b045` (2026-05-05) — they remain pinned to the 2026-04-06 / 2026-04-23 snapshots. So while the prior two reports could say "HEAD unchanged, drift identical," that is no longer true: new UI modules shipped, the Prisma schema is being modified again (uncommitted), and the docs fell further behind.

A separate `ROADMAP.md` now exists at repo root (untracked) and `docs/audits/` contains build-watchdog reports through 2026-06-02 — neither is referenced by PRD/architecture's "Related Documents" lists.

### Critical-path callouts

- **Oura sync** (`src/lib/sync.ts`): `syncOuraData()` ingests **12 endpoints** (readiness, sleep, stress, heartrate, activity, spo2, tags, workouts, sessions, sleepTime, resilience, vo2max). PRD §4.1 (lines 156–162) still lists only **5**. **Still drifted.**
- **Baseline Score calculation** (`src/lib/baseline-score.ts`): weights 40/25/20/15 confirmed (lines 94–97), null-component redistribution confirmed (lines 92–120), luteal `−0.4°C` at line 67, ovulation `−0.15°C` at line 70. Matches PRD §3.1 and architecture data-flow exactly. **No drift.**
- **Coach context builder** (`src/lib/coach-context.ts`): now **1,338 lines** (PRD §3.4 line 109 still says "541-line"), **22 `prisma.*` calls** (architecture.md lines 128 & 217 both still say "14-query"). This file is also *currently modified in the working tree*. **Still drifted, and moving.**
- **Cycle-phase logic**: `cyclePhaseGuidance("ovulation").aclWarning = true` at `src/lib/training.ts:363` (`"Peak power output, but 3-6x higher ACL injury risk (Hewett 2007)…"`); luteal/ovulation temp offsets correct in `baseline-score.ts`. PRD §3.3 line 95 still lists the ACL flag as not-yet-shipped. A dedicated `src/lib/cycle-phase.ts` ("centralized cycle-phase lookup with staleness guard") now exists and is undocumented. **Still drifted.**
- **Auth / OAuth flow**: `/api/auth/oura/route.ts` + `/api/auth/oura/callback/route.ts` present; `getValidToken()` in `src/lib/oura.ts`. Matches PRD §4.1 + architecture External Integrations table. **No drift.**

---

## Documented but missing or changed (broken promises)

### 1. PRD §3.3 "Body Mode — Features (not yet shipped)" — all seven are shipped

PRD lines 91–99 still list these under *not yet shipped*. All seven are live in code today.

| Doc claim (PRD line) | Status | Evidence |
|---|---|---|
| L93 "HRV CV as overreaching signal (UI not connected)" | **Shipped** | `src/components/body/readiness-tier-card.tsx` renders HRV CV; `src/lib/training.ts:195` `isHrvCvElevated()` drives the downgrade. |
| L94 "Deload recommendation engine (logic exists, no UI trigger)" | **Shipped** | `src/lib/training.ts:206–231` `computeFatigueScore()` → `recommendation` ("Deload recommended within 1-2 weeks" at L230, "strongly recommended" at L229); surfaced on `/body`. |
| L95 "ACL injury risk flag during ovulation (Hewett: 3–6× higher)" | **Shipped** | `src/lib/training.ts:362` `note: "…3-6x higher ACL injury risk (Hewett 2007)…"`, `aclWarning: true`; consumed by `src/components/body/cycle-phase-guidance-card.tsx`. |
| L96 "Volume zone display (MEV/MAV/MRV per muscle group)" | **Shipped** | `src/components/body/volume-zones.tsx:47` "Sets per muscle group vs MEV/MAV/MRV (Israetel 2021)". |
| L97 "Weekly sets per muscle group tracking" | **Shipped** | `/body` aggregates per-muscle sets and threads to `<VolumeZones>`. |
| L98 "Energy availability warning (<30 kcal/kg FFM)" | **Shipped** | `src/lib/training.ts:255` `energyAvailability()` (cites "Loucks 2011 — below 30 kcal/kg FFM"); rendered `src/components/weight/tdee-card.tsx:36` and `src/components/body/nutrition-check.tsx:28` (`< 30`). |
| L99 "Per-meal protein flag (>30g threshold)" | **Shipped** | `src/components/body/nutrition-check.tsx:80` `const excess = m.protein > 30;` (and `< 20` low flag at L81). |

### 2. PRD §7 Roadmap "Phase 2b" — unchecked items are shipped

PRD line 266 `- [ ] Volume zone alerts (MEV/MAV/MRV)` and line 267 `- [ ] Deload detection UI` are both shipped (see #1).

### 3. EA threshold scientific citation — verified correct, but doc under-claims

`features.md` repeatedly cites "EA < 30 kcal/kg FFM (Loucks 2011)" (lines 37, 54, 79). The implementation at `src/lib/training.ts:254–261` matches exactly, and `coach-context.ts:259–262` adds a graduated warning/critical band (`< 35` warning, `< 30` critical). This citation is **implemented as documented** — no drift, logged here as a positive confirmation since the task asks to verify cited thresholds.

### 4. PRD §3.4 Coach — context-builder line/query counts stale

PRD line 109 "541-line context builder"; actual `coach-context.ts` = **1,338 lines**. architecture.md lines 128 & 217 say "14-query"; actual = **22 `prisma.*` calls**. Undocumented coach surfaces: `detectTradeoffs()` (+ `/api/coach/tradeoffs` route), `goalSystemPromptSection()`, goal-relevance lenses, Hyrox plan integration, and a separate `src/lib/coach-tools.ts`.

### 5. PRD §3.5 / features.md §/goals — goal type taxonomy mismatch

PRD line 119 "Goal types: weight, race, exam, performance, habit, custom". `features.md` line 203 says `race | strength | physique | cognitive | weight | health | custom`. These two docs **disagree with each other**, and PRD's `exam`/`performance`/`habit` are not the live enum. The subtype list (`hyrox`, `marathon`, `5k`, `powerlifting_meet`, `cut`, `bulk`, `recomp`, `cfa`, `finals`, …) appears in `features.md` but is entirely absent from PRD.

### 6. architecture.md "Database Schema (18 models)" — header wrong and list incomplete

architecture.md line 151 header says **18 models**; the section then lists **23**; the live `prisma/schema.prisma` has **40** (`grep -c '^model '`). Both the count and the enumeration are stale (see silent-features #1 for the 17 missing models).

### 7. architecture.md application-structure tree — counts and entries stale

- Line 76 `schema.prisma  # 18 models, ~300 lines` — actual **40 models, 658 lines**.
- Lines 110–118 component counts (dashboard 7, mind 8, body 6, coach 1, goals 1, weight 5) — actual **dashboard 21, body 10, mind 10, goals 2** plus a top-level `src/components/hyrox-summary-card.tsx`.
- Line 128 `coach-context.ts # 14-query context builder` — see #4.
- The `lib/` listing (lines 119–132) omits **18** modules now present (see silent-features #3).
- No `body/hyrox/` route appears in the tree (lines 84–90) although `src/app/body/hyrox/page.tsx` exists and `features.md` documents `/body/hyrox` in full.

### 8. PRD §4.5 / architecture — Apple HealthKit marked "Planned (Phase 3)" but partially shipped

PRD line 187 "Apple HealthKit (Planned — Phase 3)" and architecture line 251 "Planned (Phase 3)". Yet `src/app/api/healthkit-sync/route.ts`, `src/app/api/workouts/apple-watch/route.ts`, `src/components/dashboard/healthkit-status.tsx`, and the `HealthKitSync` / `HealthKitWorkout` / `HeartRateZoneSummary` models all exist. `features.md` line 18 even documents a "HealthKit status" panel on the dashboard. **HealthKit is at least partially shipped; PRD/architecture still call it unplanned.**

---

## Shipped but undocumented (silent features)

### 1. Seventeen Prisma models absent from architecture.md schema section

Present in `prisma/schema.prisma`, missing from architecture.md lines 153–187:
`HyroxPlan`, `HyroxSession`, `HyroxStationBenchmark`, `GoalWorkoutTag`, `HealthKitSync`, `HealthKitWorkout`, `HeartRateZoneSummary`, `DailySpO2`, `OuraWorkout`, `OuraSession`, `SleepTimeRecommendation`, `DailyResilience`, `DailyRunningMetrics`, `DailyVO2Max`, `LifeContextDef`, `LifeContextLog`, `WorkoutNote`. *(`prisma/schema.prisma` is currently modified in the working tree — model count may still be moving.)*

### 2. Twelve undocumented API routes

Not in architecture.md's `/api` tree (lines 96–109):
`coach/tradeoffs`, `healthkit-sync`, `hyrox/plan`, `hyrox/plan/[id]`, `hyrox/sessions`, `hyrox/today`, `life-context/defs`, `life-context/logs`, `workout-notes` (+ `[id]`, `[id]/analyze`), `workouts/apple-watch`, `workouts/manual`.

### 3. Eighteen undocumented `lib/` modules

Beyond the 13 listed in architecture.md lines 119–132:
`anthropic-retry`, `coach-tools`, `cycle-phase`, `flags` ("the 'this doesn't add up' surface"), `gi-classifier`, `hyrox-archive`, `hyrox-blocks`, `hyrox-constants`, `hyrox-pace`, `hyrox-plan-sync`, `hyrox-session-recommender`, `hyrox-today`, `pre-workout-fuel`, `today-call` (**marked `@deprecated`** in its own header — superseded by `training.ts` / `training-call.ts`, but still in tree), `training-call`, `utils`, `workout-discussion`, `workout-notes`.

### 4. Entire Hyrox module undocumented in PRD & architecture

`/body/hyrox` page, four `/api/hyrox/*` routes, eight `hyrox-*` lib modules, three Hyrox Prisma models, and `src/components/hyrox-summary-card.tsx` + `src/components/body/start-hyrox-session-button.tsx` + `src/components/dashboard/hyrox-countdown-card.tsx`. **Only `features.md` documents it** (`/body/hyrox`, lines 84–95); PRD and architecture.md never mention Hyrox as a product surface despite it being the user's primary training goal.

### 5. "Athletic redesign" dashboard components (new since last report)

The 2026-05-29→05-31 commits added dashboard modules absent from architecture's "dashboard (7)" list: `today-call-card`, `today-call-hero`, `do-section`, `tonight-section`, `baseline-card`, `sleep-ring`, `typewriter-reveal`, `workout-notes-block`, `manual-workout-entry`, `cycle-card`. Backed by `src/lib/today-call.ts` and `src/lib/training-call.ts` (the integrated "training call" surface — also undocumented).

### 6. Life-context & flags surfaces (Mind Mode)

`src/components/mind/life-context-card.tsx` + `flags-feed.tsx`, backed by `LifeContextDef`/`LifeContextLog` models, `/api/life-context/*` routes, and `src/lib/flags.ts`. Not in PRD §3.2's Mind Mode feature list.

### 7. Workout-notes feature

`src/components/dashboard/workout-notes-block.tsx`, `WorkoutNote` model, three `/api/workout-notes/*` routes (including an `analyze` endpoint), and `src/lib/workout-notes.ts` / `workout-discussion.ts`. Undocumented across all three source-of-truth docs.

### 8. Untracked sibling docs not in "Related Documents"

`ROADMAP.md` (repo root) and `docs/audits/build-watchdog-*.md` (through 2026-06-02) are not referenced in PRD §Appendix or architecture's docs tree. `features.md` also references `research/body-mode-research.md`, `research/variable-research.md`, `docs/hyrox-module-spec.md`, `docs/goal-coach-redesign-spec.md` — confirm these exist; PRD's appendix points to `body-mode-research.md` at repo-relative path without the `research/` prefix (path drift between PRD line 295 and features.md line 4).

---

## Methodology

Verified `src/app/` pages + API routes, `src/components/`, `src/lib/`, and `prisma/schema.prisma` against the three named docs via file enumeration and targeted `grep` for each cited threshold/formula (EA < 30, per-meal protein > 30, HRV CV, Epley e1RM, baseline-score weights, cycle-phase temp offsets, ACL/Hewett). Counts (`wc -l`, `grep -c`) taken at HEAD `80a25d8` with 10 files modified in the working tree; coach-context and schema counts may shift on next commit. No code or docs were modified by this run.
